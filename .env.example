# ก๊อปไฟล์นี้เป็น .env.local แล้วเติมค่า — .env / .env.local อยู่ใน .gitignore แล้ว
# ค่าจริงและคำอธิบายอยู่ใน docs/06-supabase-setup.md

# ── เปิดเผยได้ อยู่ในหน้าเว็บอยู่แล้ว ────────────────────────────────
# ความปลอดภัยมาจาก RLS ที่ฐานข้อมูล ไม่ได้มาจากการซ่อนคีย์นี้
NEXT_PUBLIC_SUPABASE_URL=https://hpsmjavfvrdctclmlmhp.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# ── ห้ามหลุดเด็ดขาด ใช้ฝั่งเซิร์ฟเวอร์เท่านั้น ─────────────────────
# คีย์นี้ข้ามผ่าน RLS ทุกข้อ ใครได้ไปอ่านและแก้ข้อมูลทุกสาขาได้ทั้งหมด
# ห้าม commit · ห้ามใส่ในหน้าเว็บ · ห้ามส่งทางแชต
SUPABASE_SERVICE_ROLE_KEY=

# ── ต่อฐานข้อมูลตรง (migration / งานตามเวลา) ────────────────────────
DATABASE_URL=

# ── LINE Messaging API (Phase 2) ───────────────────────────────────
# แพ็กฟรีส่งได้ 300 ข้อความ/เดือน เกินแล้วต้องขึ้นแพ็ก Basic
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=
# ปลายทาง push แจ้งเตือนภายในร้าน (groupId/userId ของฝ่ายร้าน) — ไม่ตั้ง = cron ไม่ส่ง LINE (no-op)
LINE_NOTIFY_TO=

# ── งานตามเวลา (cron E10) ──────────────────────────────────────────
# เรียก /api/cron/* ด้วย header Authorization: Bearer <CRON_SECRET> · ไม่ตั้ง = endpoint ตอบ 401 ทุกครั้ง
CRON_SECRET=

# ── AI (E12) ───────────────────────────────────────────────────────
# คีย์ Claude ฝั่ง server เท่านั้น (ห้าม NEXT_PUBLIC · ห้าม commit) · ไม่ตั้ง = /api/ai/* ปิด
ANTHROPIC_API_KEY=
# เปิดฟีเจอร์ AI: ต้องเป็น "true" **และ** มี ANTHROPIC_API_KEY ถึงจะทำงาน (ไม่งั้น 503)
AI_ENABLED=false

# ── โหมดทดลอง (ให้ลูกค้าลองก่อนเปิดจริง) ───────────────────────────
# DEMO_LOGIN=true → หน้า login ใส่อะไรก็ได้แล้วเข้าเป็นบัญชีทดลองด้านล่าง (เห็นข้อมูลจริงผ่าน RLS)
# **ปิด (ลบ/false) ก่อนเปิดใช้จริง** เพราะข้ามการตรวจรหัส · คีย์อยู่ฝั่ง server เท่านั้น (ไม่ NEXT_PUBLIC)
DEMO_LOGIN=false
DEMO_LOGIN_EMAIL=admin@famai.local
DEMO_LOGIN_PASSWORD=
