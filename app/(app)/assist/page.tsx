import { AssistChat } from "@/components/ai/AssistChat";

export const metadata = { title: "ผู้ช่วยวิเคราะห์ — Famai Motor Group" };

export default function AssistPage() {
  return <AssistChat />;
}
