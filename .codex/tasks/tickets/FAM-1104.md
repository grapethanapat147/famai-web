# FAM-1104 · หน้ารายงาน — กดดูรายละเอียดแต่ละรายการ (drill-down)

**ประเภท:** (feature) · **ที่มา:** เกรพ ("ในหน้ารายงาน มันจะต้องกดเข้าไปดูรายละเอียดในแต่ละรายการได้ด้วย")

## ปัญหา
หน้า /report แสดงเป็น **ยอดสรุปรายกลุ่ม** (ตามรุ่น/บริษัท/พนักงาน/เดือน · หมวด · ประเภทเงินค้าง) — กดดูไม่ได้ว่ากลุ่มนั้นมาจากรายการอะไรบ้าง

## ทำ ✅ (branch feature/FAM-1104-report-drilldown) — **ไม่ต้อง migration**
- แต่ละแถวสรุป **กดได้** (cursor + chevron › + hint "แตะแถวเพื่อดูรายละเอียด") → เปิดโมดัลรายการดิบของกลุ่มนั้น
- โมดัลแสดงรายการจริงตามช่วงวันที่/ตัวกรองปัจจุบัน:
  - ยอดขาย: วันที่ · รุ่น · บริษัท · พนักงาน · ยอดสุทธิ · (กำไร เฉพาะผู้มีสิทธิ์เห็นเงิน)
  - ค่าใช้จ่าย: วันที่ · หมวด · ยอด
  - เงินค้างรับ: ประเภท · ยอดค้าง
- keyboard a11y (Enter/Space) · โมดัลเลื่อนได้ (max-h 60vh, หัวตารางติดบน)
- money-strip เดิมคงอยู่ (กำไรซ่อนเมื่อไม่มีสิทธิ์) · ข้อมูลดิบมีในหน้าอยู่แล้ว (ไม่เพิ่ม query)

## โครงสร้าง/เทส
- `lib/report/aggregate.ts`: เพิ่ม `groupMembers()` — normalize key เดียวกับ `groupAggregate` (ว่าง→'—') เพื่อให้จำนวนตรงเสมอ
- `ReportView`: drill-down state + `DetailModal` (module-level) · เปลี่ยนรายงาน/กลุ่ม รีเซ็ต detail
- `tests/report.test.ts` +3 เคส (groupMembers: สมาชิก/invariant นับตรง row.count/em-dash)

## Verify
- สวีทเต็ม 378 ผ่าน · typecheck · lint · build (/report ƒ, /dev/report ○) เขียว
- (preview /dev/report) กด NMAX → โมดัล "ยอดขาย · NMAX · 2 รายการ" = 92,000+90,000 ตรงกับยอดกลุ่ม 182,000 ✓
