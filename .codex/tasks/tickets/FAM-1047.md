# FAM-1047 · UI polish — เจาะหน้าใบเสนอราคา (QuoteView)

**Epic:** FAM-E13 (UI/ธีม track — polish รอบ 6)
**สถานะ:** In Progress (branch feat/quote-polish)
**ที่มา:** เกรพ — "ทำ quote ต่อ"

## แนวคิด
หน้า quote โตแล้ว (list = DataTable ที่ขัด, builder + CompareTable ใช้ได้) → เติม craft เฉพาะจุดที่ช่วยการขาย + ทำให้ตารางเทียบ (ที่ลูกค้าเห็น/แคป/พิมพ์) อ่านง่ายขึ้น

## ทำอะไร (logic/computeDeal เดิม ไม่แตะ)
- **การ์ดคันที่ 1/2 — hint ราคาตั้ง** — เลือกรุ่นแล้วโชว์ "ราคาตั้ง ฿X" ใต้ช่องราคา · ถ้าตั้งราคาต่ำกว่า → "· ลด ฿Y" (เขียว) · สูงกว่า → "สูงกว่าราคาตั้ง" (เหลือง) → คนขายเห็นส่วนลดชัด
- **CompareTable header** — เพิ่ม eyebrow "ตัวเลือก 1/2" + ชื่อรุ่นเป็น font-display semibold + เส้นคั่นหนาขึ้น → คอลัมน์เทียบชัด (ดีต่อรูปที่แคป/พิมพ์)
- **แถว "ยอดจัด"** — ไฮไลต์พื้นอ่อน (bg-paper-2) เป็นตัวเลขหลักที่ลูกค้าดู

## Verify (/dev/quote → สร้างใบเสนอราคา)
- เลือก 2 รุ่น + ราคา → hint "ราคาตั้ง ฿92,000" · ตั้ง 85,000 → "ราคาตั้ง ฿92,000 · ลด ฿7,000" (เขียว) · ค่างวดคำนวณสด (8,095)
- CompareTable: header "ตัวเลือก 1 / NMAX", "ตัวเลือก 2 / Aerox" + ยอดจัดไฮไลต์
- mobile 375 ไม่มี page h-scroll (ตารางเลื่อนในตัวเอง) · dark สะอาด
- tsc + eslint + vitest (235) + build ผ่าน
