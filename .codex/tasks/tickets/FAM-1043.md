# FAM-1043 · Dashboard ต่อ — mini per-branch split + รายการรถค้าง

**Epic:** FAM-E13 (UI/ธีม track — polish รอบ 3)
**สถานะ:** In Progress (branch feat/dashboard-aged-list)
**ที่มา:** เกรพ — "ดัน Dashboard ต่อ (mini-branch split / รายการรถค้าง)" (follow-up FAM-1042)

## ทำอะไร
### Part 1 — mini per-branch split (UI ล้วน, data เดิม)
- เติมฝั่งขวาแถบพระเอกที่เคยว่าง → "แยกตามสาขา" (ชื่อสาขา + แท่งสัดส่วน + จำนวน) จาก `stats.byBranch`
- แสดงเมื่อมี ≥2 สาขาในมุมมองปัจจุบัน (เลือกสาขาเดียว → ซ่อนเอง) · sm+ อยู่ข้างตัวเลขมีเส้นคั่น · mobile ต่อท้ายด้านล่าง
- **ยุบการ์ดกราฟ "สต๊อกแยกตามสาขา" เดิม** (ซ้ำกับ split) → เหลือที่ให้การ์ดใหม่

### Part 2 — การ์ด "รถค้างนานสุด" (เพิ่ม data นิดหน่อย)
- `agedUnits(units, agingDays, limit)` (pure, ใน stats.ts) — คันในสต๊อกที่เกินเกณฑ์ เรียงค้างนานสุด + tests (+2)
- ขยาย `DashUnit` เพิ่ม `id` + `model` · query /dash join `model_variant.model_name` (+ dev sample ใส่ชื่อรุ่นจริง)
- การ์ดโชว์ top 5: ชื่อรุ่น · สาขา · อายุ (แดง) + ลิงก์ "ดูสต๊อกทั้งหมด →" (/stock; unit detail เป็น client panel ไม่มี route เดี่ยว) · empty state ถ้าไม่มีค้าง
- จับคู่กับกราฟ "ช่วงอายุสต๊อก" ในแถวล่าง (อายุรวม + คันที่ค้างจริง)

## Verify (/dev/dash)
- split: ทุกสาขา → FMG 4 / FMM 3 / FCG 2 · เลือก "Famai Motor" (สาขาเดียว) → split ซ่อน + aged list เหลือ R15 M 120 วัน
- aged list: XMAX 300 200วัน · R15 M 120วัน · NMAX 155 95วัน (เรียงถูก, top 5, อายุแดง) · ลิงก์ href=/stock
- mobile 375 ไม่มี h-scroll (hero/split stack) · dark สะอาด · console สะอาด
- tsc + eslint + vitest (235) + build ผ่าน
