# FAM-1051 · หน้ารายละเอียดรุ่น + พร้อมรูปจริง (E11 slice 2)

**Epic:** FAM-E11 (Public Catalog & Status)
**สถานะ:** Done (branch feat/catalog-detail)
**ที่มา:** เกรพ — "ทำ E11 ต่อ รายละเอียดรุ่น/รูปจริง"

## หมายเหตุเรื่องรูป
เช็ก pub.model จริง: 14 รุ่น **ยังไม่มีรูปเลย** (photo/photos = null ทั้งหมด) → "รูปจริง" = ต่อ **ท่อแสดงผลรูป** ให้พร้อม พออัปโหลดผ่านหน้าแอดมิน (models, FAM-1024) → รูปขึ้นเองทั้งแคตตาล็อก+รายละเอียด · ระหว่างนี้ใช้ placeholder อักษรย่อ · แต่ **สีจริงมี** (1–4 สีต่อรุ่น)

## ทำอะไร (อ่านอย่างเดียว)
- `lib/catalog/model.ts` — เพิ่ม `modelPhotoUrl` (path→bucket url) + `galleryImages` (photos[]→{thumb:card, full} · fallback ปก · [] ถ้าไม่มี) · refactor catalogPhotoUrl ให้ใช้ร่วม + tests (+5)
- **`/catalog/[code]`** (public) — server fetch pub.model by code (`.maybeSingle`, ไม่พบ→`notFound()` 404) + **generateMetadata แบบ dynamic** (title/description = ชื่อรุ่น+ราคา, SEO)
- `ModelDetail` (client) — แกลเลอรี (รูปหลัก + thumbnail สลับได้ · placeholder ถ้าไม่มีรูป) + ข้อมูล: ประเภท/ชื่อ/ซีซี·ปี/ราคา/ป้ายมีจำหน่าย/**สีที่มี (pill ชื่อสีจริง)** + CTA (ดูรุ่นอื่น / เช็กสถานะ)
- การ์ดแคตตาล็อก → Link `/catalog/[code]` + hover (เงา + ซูมรูป group-hover)

## Verify
- /catalog/B6FU00 → ฟินน์ ล้อแม็ก · Moped · 114 ซีซี · ปี 2567 · 46,900฿ · มีจำหน่าย · 3 สี (น้ำเงิน/เขียว/ฟ้า) · title tab = ชื่อรุ่น (SEO) · gallery placeholder (ยังไม่มีรูป)
- /catalog/NOTEXIST → 404 · การ์ด 14 ใบลิงก์ `/catalog/<code>` ครบ
- mobile 375 ไม่มี page h-scroll · dark สะอาด · console สะอาด · tsc + eslint + vitest (246) + build ผ่าน

## ตามมา
- อัปโหลดรูปรุ่นผ่านแอดมิน (models) → catalog/detail โชว์รูปจริงทันที · sitemap /catalog · ปุ่มแชร์/โซเชียล meta (og:image)
