# FAM-1050 · หน้าเว็บลูกค้า — แคตตาล็อก + เช็กสถานะ (E11 slice 1)

**Epic:** FAM-E11 (Public Catalog & Status · pub schema)
**สถานะ:** Done (branch feat/public-catalog)
**ที่มา:** เกรพ — "ปิด initiative เจาะหน้า ไปงานที่ค้างต่อ" → E11 เป็นงานค้างที่ backend พร้อม (migration 14) ทำได้เลย

## ทำไม E11
- งานค้างส่วนใหญ่ block รอ input (FAM-1022 บริษัท/tax id, login creds) หรือ infra (E10 LINE token)
- **E11 backend ครบแล้ว** — `pub.model` view + `pub.order_status(token)` RPC (migration 14) · verify anon อ่านได้จริง (200) · เหลือแค่ front-end

## ทำอะไร (อ่านอย่างเดียว ไม่มี write)
- `lib/supabase/public.ts` — public client (ไม่ผูก session, default schema=pub) สำหรับ pub.model / pub.order_status
- `lib/catalog/model.ts` (pure) — type CatalogModel + `availabilityMeta` (ready/low/order → มีจำหน่าย/เหลือน้อย/สั่งจอง) + `catalogPhotoUrl` (build URL จาก model-photo bucket) + tests (+6)
- **`/catalog`** (public, นอก (app) auth group) — server component อ่าน pub.model → `CatalogView`: กริดรุ่น (รูป/ชื่อ/ประเภท·ซีซี·ปี/ราคา/จำนวนสี/ป้ายมีจำหน่าย) + กรองตามประเภท + ค้นชื่อ · SEO metadata · revalidate 300
- **`/status`** (public, `robots: noindex`) — `StatusCheck`: กรอกรหัสติดตาม → เรียก pub.order_status → โชว์สถานะ (rate-limit/privacy คุมที่ RPC แล้ว: ไฟแนนซ์ไม่ผ่าน→"กรุณาติดต่อร้าน", เบอร์ถูก mask) · ไม่พบ→การ์ด "ไม่พบรหัสนี้"
- แยก public จาก admin สนิท: routes อยู่นอก `(app)` (auth guard อยู่ใน (app)/layout เท่านั้น) · pub schema เป็นเส้นแบ่งความปลอดภัย (ไม่มีต้นทุน/กำไร/เลขเครื่อง/จำนวนคันหลุด)

## Verify
- /catalog: อ่าน pub.model จริง → FINN/NMAX/Aerox/... · กรอง Moped → เหลือเฉพาะ Moped · ป้าย มีจำหน่าย/เหลือน้อย · ราคา/สอบถามราคา (retail null) · รูป null → placeholder ตัวอักษร
- /status: token มั่ว → "ไม่พบรหัสนี้" (RPC ยิงจริง 200, ไม่ error)
- mobile 375 ไม่มี page h-scroll · dark สะอาด · console สะอาด · tsc + eslint + vitest (241) + build ผ่าน

## ตามมา (E11 ต่อ)
- รูปจริง (อัปโหลด model_photo ให้ครบ) · หน้ารายละเอียดรุ่น (สี/สเปก) · ลิงก์แชร์ · sitemap/robots สำหรับ /catalog
