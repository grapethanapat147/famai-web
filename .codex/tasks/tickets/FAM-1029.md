# FAM-1029 · แม่แบบพิมพ์ใบเสนอราคา + ดู/แก้/แปลงเป็นการขาย (quote follow-ups)

**Epic:** FAM-E08 (ลูกค้าและการเงิน)
**สถานะ:** Done (แม่แบบพิมพ์ + ดู/แก้/ทำสำเนา/ลบ + ดอกเบี้ย tiered) — แปลงเป็นการขาย + >2คัน = ตามมา
**ขึ้นกับ:** FAM-1014 (หน้า quote ตอนนี้ list + สร้าง/เทียบ/บันทึก + พิมพ์ดิบ ๆ)

## เป้าหมาย
เติมส่วนที่ FAM-1014 กันไว้ให้ครบ

## ขอบเขต
- **แม่แบบพิมพ์เต็ม** ✅: หัวกระดาษ (Famai Motor Group Yamaha) + ข้อมูลผู้ขาย/สาขา (ชื่อ/ที่อยู่/เบอร์/เลขภาษี, R1 ขายรถ) + เลขที่/วันที่/ยืนราคา + ลูกค้า + ตารางเทียบสวย + ผู้เสนอราคา/ลายเซ็น
  - print CSS แยกเอกสารจาก chrome (`.print-doc` + `@media print` visibility isolation) · ปุ่ม **ดูตัวอย่าง** เปิดพรีวิวบนจอ · **พิมพ์** = `window.print()`
- **ดู/แก้ใบที่บันทึกแล้ว** ✅ (PR feat/quote-view): คลิกแถว→เปิดใน builder โหมดแก้ (prefill ครบ) · ทำสำเนา · ลบ (สองสเต็ป)
- **แปลงใบเสนอราคา → การขาย** (ส่งต่อ FAM-1011 พร้อมคัน/ราคา/ไฟแนนซ์ที่เลือก) — *บล็อกรอ sell_unit RPC (migration 17) เหมือน /sell*
- **ดอกเบี้ยแบบช่วงงวด (tiered)** ✅ (R1 B2 · `finance_company.rate_tiers`, PR feat/quote-tiered)
- เทียบมากกว่า 2 คัน — *ตามมา*

## ดอกเบี้ย tiered (Done, PR feat/quote-tiered)
- `finance_company.rate_tiers` (jsonb `{"12":1.29,"36":1.45}`) มีจริงตั้งแต่ migration 10 (applied) แต่ยังไม่ typed → เพิ่มใน database.types
- lib/quote/finance.ts: `parseRateTiers` (คัดคีย์จำนวนงวด→เรตตัวเลข, ว่าง→null) + `rateForTerm` (tier ถ้ามี ไม่งั้น flat) + `hasRateTiers` + `financeLabel` (มี tier→"เรตตามงวด") + `optionTerms` รับ param `tiers` (optional, backward-compatible) + tests (+5)
- QuoteFinanceCo/PrintBuilt เพิ่ม rateTiers · CompareTable + PrintableQuoteDoc ใช้ financeLabel · quote/page ดึง rate_tiers + parseRateTiers
- verify /dev/quote: กรุงศรี tiered {"12":1.19,"24":1.35,"48":1.59} → ค่างวด 12/24/48 ใช้ tier (7,809/4,524/3,012), 18/36 fallback flat 1.35 (5,663/3,385), ทิสโก้ (flat) ไม่กระทบ, ป้าย "เรตตามงวด" ทั้งตารางจอ+พิมพ์, mobile ok, console สะอาด · vitest 198 ผ่าน

## ดู/แก้/ทำสำเนา/ลบ (Done, PR feat/quote-view)
- helper `savedOptionsToSlots` (lib/quote/quotes.ts) map saved options→2 slots (เรียง slot, เติมช่องว่าง) + tests (quote.test.ts +3) · types `SavedQuote`/`SavedQuoteOption`/`QuoteOptionInput`
- actions `updateQuote` (คงเลขเอกสาร/วันที่ออก · อัปเดตลูกค้า/ยืนราคา + แทนที่ options ลบแล้วใส่ใหม่) + `deleteQuote` (ลบจริง — ไม่มี soft-delete col · gate canManageQuote)
- QuoteView: แถวรายการคลิกได้ → เปิด QuoteBuilder โหมดแก้ (`key={id}` remount + init state จาก editing · terms คำนวณสดจึงไม่ต้อง restore) · ปุ่ม อัปเดต/ทำสำเนา (edit+canManage) · ลิงก์ลบสองสเต็ป · print doc ใช้เลข/วันที่จริงในโหมดแก้
- page: quotation_option โหลดคอลัมน์เต็ม + build SavedQuote[] · dev/quote mock update/delete + options
- verify /dev/quote: เปิดใบ 2 คัน prefill ครบ (รุ่น/ราคา/ดาวน์/ไฟแนนซ์), ใบเงินสด 1 คัน slot2 ว่างถูกต้อง, อัปเดต/ทำสำเนา note ถูก, ลบสองสเต็ป→กลับรายการ, mobile 375 ไม่มี h-scroll, console สะอาด · tsc+eslint+build ผ่าน · vitest 193 ผ่าน · เขียนจริงยังไม่ทดสอบ (ล็อกอินยังไม่ได้)

## หมายเหตุ
~~ตอนนี้ปุ่ม "พิมพ์" = `window.print()` + `print:` utilities เบื้องต้น~~ → ทำแม่แบบจริงแล้ว
- helper ตรรกะล้วน `lib/quote/print.ts` (`quotePrintColumns`/`monthlyFor`) + เทส `tests/quote-print.test.ts`
- คอมโพเนนต์ `components/quote/PrintableQuoteDoc.tsx` · seller ดึงจาก `branch`+`company`+ผู้ใช้ปัจจุบัน
- verify เต็มบน `/dev/quote` (พรีวิว + isolation + mobile 375 ไม่มี h-scroll) — flow บันทึกจริงยังไม่ได้ทดสอบเพราะยังล็อกอินไม่ได้
