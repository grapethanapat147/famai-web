# FAM-1100 · หน้างานทะเบียน — คิวป้าย 2 ระยะ (ยื่นขนส่ง → รับเล่ม)

**ประเภท:** (feature) · **ที่มา:** เกรพ (เลือกจาก 4 ตัวเลือก) — คิวงานทะเบียนแยกให้ทีมธุรการไล่ตามค้างนาน

## ⚠️ ต้องรัน migration ก่อน merge
`supabase/migrations/20260823140000_24_registration_plate.sql` — เพิ่ม 2 คอลัมน์บน `registration`:
`dlt_request_no text`, `dlt_submitted_at date` (plate_no/book_no/plate_received_at มีอยู่แล้ว) · idempotent (`if not exists`) · RLS เดิม (for all) ครอบคลุม

## ทำ ✅ (branch feature/FAM-1100-registration-queue)
- เมนูใหม่ **"งานทะเบียน"** (กลุ่มลูกค้าและการเงิน · admin/manager/acct) → หน้า `/registration`
- **คิวงาน** = ดีลขั้น `รอทะเบียน` + `ป้ายขาว` · เรียงค้างนานสุดอยู่บน · สรุปนับ 3 ระยะ · ค้นหา + กรองตามระยะ
- **3 ระยะ** (คำนวณจากข้อมูลจริง): รอยื่นขนส่ง → รอเล่มทะเบียน → ได้ป้ายแล้ว · ป้ายสี + อายุ (⚠️ เมื่อ ≥ 10 วัน)
- **บันทึกเลขคำขอ** (`recordDltSubmission`) — เก็บเลขคำขอ + วันยื่น (ยังขั้นรอทะเบียน)
- **รับเล่มแล้ว** (`recordPlateReceived`) — เก็บเลขทะเบียน/เล่ม แล้ว **เลื่อนดีล รอทะเบียน → ป้ายขาว** (CAS ตรงกับไปป์ไลน์ดีล) · log registration_event
- อายุนับจาก วันยื่นขนส่ง → ไม่มีก็ วันอนุมัติ → ไม่มีก็ วันขาย · กรองดีลที่ยกเลิกออก

## โครงสร้าง/เทส
- `lib/registration/plate.ts`: canManagePlate · platePhase · plateWaitingSince · plateAgeDays · phaseVariant · validateDltRequest/validatePlateReceived (pure)
- actions + page + `RegistrationView` (DataTable + 2 modal) + `/dev/registration` sample
- `database.types`: +registration.dlt_* · +RegistrationEvent · menu 22→23 (อัปเดต menu.test)
- `tests/plate.test.ts` (20+ assert) · สวีทเต็ม 387 ผ่าน · typecheck · lint · build (/registration ƒ) เขียว
- (preview /dev/registration) คิว 3 ระยะเรียงอายุ (12⚠️/5/2) + โมดัลบันทึกเลขคำขอเปิดได้ ✓

## ยังไม่ทำ
- แจ้งเตือน overdue (มี /api/cron/registration-overdue อยู่แล้ว — อนาคตเชื่อม)
