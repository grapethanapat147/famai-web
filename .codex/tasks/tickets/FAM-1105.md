# FAM-1105 · ส่งมอบรถ — ปิดวงจรดีล (ป้ายขาว → ส่งมอบแล้ว)

**ประเภท:** (feature) · **ที่มา:** เกรพ (สเปคเดิม deal P4: วันที่/สถานที่/ผู้ส่งมอบ) — ต่อจาก FAM-1100

## ⚠️ ต้องรัน migration ก่อน merge
`supabase/migrations/20260823150000_25_registration_delivery.sql` — เพิ่ม 2 คอลัมน์บน `registration`:
`delivery_place text`, `delivered_by uuid references app_user(id)` (delivered_at มีอยู่แล้ว) · idempotent · RLS เดิมครอบคลุม

## ทำ ✅ (branch feature/FAM-1105-delivery)
- ต่อยอดหน้า **งานทะเบียน** — แถว "ได้ป้ายแล้ว" (ขั้นป้ายขาว) เปลี่ยนจากป้าย "รอส่งมอบ" เป็นปุ่ม **[ส่งมอบ]**
- โมดัลส่งมอบ: **วันที่ส่งมอบ** (ดีฟอลต์วันนี้) · **สถานที่ส่งมอบ** · **ผู้ส่งมอบ** (ดรอปดาวน์พนักงาน active)
- `recordDelivery` — เก็บ delivered_at/place/by แล้ว **เลื่อนดีล ป้ายขาว → ส่งมอบแล้ว** (CAS) · log registration_event
- ส่งมอบแล้ว → ดีลปิด, หลุดจากคิว (คิว = รอทะเบียน + ป้ายขาว)

## โครงสร้าง/เทส
- `lib/registration/plate.ts`: +validateDelivery (วันที่บังคับ, place/person ว่าง→null) + StaffOption
- actions: +recordDelivery · page: โหลด app_user (active) เป็นตัวเลือกผู้ส่งมอบ · RegistrationView: +DeliveryModal
- database.types: +registration.delivery_place/delivered_by · ไม่แตะเมนู (ต่อยอดหน้าเดิม)
- `tests/plate.test.ts` +validateDelivery · สวีทเต็ม 388 ผ่าน · typecheck · lint · build (/registration ƒ) เขียว
- (preview /dev/registration) แถวได้ป้าย → [ส่งมอบ] → โมดัลครบ (วันที่/สถานที่/ผู้ส่งมอบ + ดรอปดาวน์พนักงาน) ✓

## วงจรดีลครบแล้ว
ขาย → ไฟแนนซ์(ผ่าน/ไม่ผ่าน) → รอทะเบียน → ยื่นขนส่ง → รับเล่ม/ป้าย → **ส่งมอบ** ✅
