# FAM-1061 · E08 — surface deals ที่ต้องจัดการ (hero + off-track filter)

**Epic:** FAM-E08 (Customer & Deal) · **สถานะ:** In Progress (branch feat/deal-attention)
**ที่มา:** เกรพ — "ไป E08 Customer & Deal ต่อ"

## แนวคิด
DealView มี pipeline ครบแล้ว (คำนวณจากใบขาย/ทะเบียน/เคสสินเชื่อจริง ไม่เก็บสถานะซ้ำ) — แต่ **ดีลตกราง (ไฟแนนซ์ปฏิเสธ) ไม่ถูกดันให้เห็นชัด** (มีแต่ chip แยกขั้น) → เพิ่มการ surface + drill-in

## ทำอะไร (logic pipeline เดิม ไม่แตะ)
- **`lib/deal/deals.ts`** — `offTrackCount(deals)` (นับไฟแนนซ์ปฏิเสธ) + `filterDeals` รับ `onlyOffTrack` + tests
- **DealView hero** — 3 การ์ด: ดีลทั้งหมด · ยังไม่ส่งมอบ (openDealCount) · **ต้องจัดการ (accent ถ้า >0)** = ไฟแนนซ์ปฏิเสธ
- **ตัวกรอง** — รวมเป็น chip เดียว: ทั้งหมด / ยังไม่ส่งมอบ / **ต้องจัดการ (N)** (จาก onlyOpen boolean → view state)

## Verify (/dev/deal)
- hero: ทั้งหมด 4 · ยังไม่ส่งมอบ 3 · **ต้องจัดการ 1** (accent) — สอดคล้อง sample (ประเสริฐ ไฟแนนซ์ปฏิเสธ)
- กด chip "ต้องจัดการ (1)" → เหลือ 1 แถว (ประเสริฐ · XMAX 300)
- mobile 375 ไม่มี h-scroll · pure logic เทส (offTrackCount, onlyOffTrack filter)
- tsc + eslint + vitest (281) + build ผ่าน

## E08 สถานะ
pipeline (ขั้นทะเบียน/เคสสินเชื่อ/void) + AR (เงินค้างรับ) มีแล้ว · ใบนี้เพิ่มการ surface ดีลตกราง · เตือนทะเบียนค้าง = cron FAM-1057
