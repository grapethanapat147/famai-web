# FAM-1024 · อัปโหลดรูปรถเข้า storage (model-photo)

**Epic:** FAM-E03 (มอเตอร์ไซค์ / สต๊อก)
**สถานะ:** Done (PR feat/model-photo) — bucket มากับ migration 13 (apply แล้ว)
**ขึ้นกับ:** FAM-1009 (หน้ารุ่นรถและสี — แสดงรูปได้แล้ว ยังอัปโหลดไม่ได้) · migration 13 (bucket model-photo)

## เป้าหมาย
อัปโหลดรูปปกรุ่นรถ — ย่อฝั่งเครื่องเป็น WebP (quality ดีแต่ประหยัดพื้นที่ ตาม migration 13) แล้วเก็บใน bucket

## ขอบเขต (Done)
- **lib/models/image.ts**: `fitDimensions` (คงสัดส่วน ไม่ขยายเกิน — ทดสอบได้) + `resizeToWebp` (canvas → WebP, ลบ EXIF/GPS) + `canUploadModelPhoto` (admin/manager = is_manager) + tests
- **client (ModelsView)**: กดรูป → PhotoModal → เลือกไฟล์ → ย่อ card 640 + full 1600 → อัปขึ้น bucket ผ่าน browser client (RLS is_manager) → เรียก action → `router.refresh()`
- **action `saveModelPhoto`**: gate canUploadModelPhoto + upsert model_photo(sort=0) + คัด photo_url ลง model_variant
- database.types: เพิ่ม model_variant.photo_url (migration 10)

## Verify
- fitDimensions: 6 tests (landscape/portrait/ไม่ขยาย/square/guard) · vitest 207 ผ่าน
- storage smoke (service_role): อัป webp ✅ · public fetch 200 ✅ · ปฏิเสธ png ✅ (bucket จำกัด webp/jpeg)
- /dev/models: กดรูป → PhotoModal เปิด + file input + ข้อความย่อ WebP · console สะอาด · tsc+eslint+build ผ่าน
- **อัปโหลดจริง (เลือกไฟล์+auth) = เกรพทดสอบเอง** (headless เลือกไฟล์ไม่ได้)

## ตามมา (ยังไม่ทำ)
- รูปหลายมุม (sort 1-3) · ลบไฟล์เก่าใน storage เมื่ออัปใหม่ (ตอนนี้ unique filename → ไฟล์เก่าค้าง) · reorder/ลบรูป
