## FAM-1002: Design tokens + Thai fonts + app shell

Status: In Progress
Priority: High
Epic: FAM-E02
Refs: docs/04-design-system.md · index.html (v1.15) · Spec §11

### Summary
พอร์ต design system เป็นฐานจริง: CSS variables/tokens (§11.1 สี, 8 token), ฟอนต์ไทย **โฮสต์เอง** (Noto Sans Thai — ตามที่ v1.1 ใช้; ไม่โหลด Google Fonts), และ app shell มือถือเป็นจอหลัก — แถบเมนูล่าง + ปุ่มขายลอย (v1.2) + topbar; เดสก์ท็อปเป็น sidebar

### Acceptance Criteria
- tokens ครบตาม §04/§11.1 ใช้ผ่าน Tailwind theme + CSS vars, รองรับสลับฟอนต์มีหัว/ไม่มีหัว (ค่าเริ่มต้น = ไม่มีหัว)
- ฟอนต์ไฟล์อยู่ในโปรเจกต์ (woff2), ไม่มี request ออก Google Fonts
- shell: มือถือ bottom-nav + ปุ่มขายลอย, เดสก์ท็อป sidebar `--ink`; ไม่มี horizontal scroll บนมือถือ (§v1.4)
- เมนูยึดโครง 20-24 หน้าจอจาก `index.html` (`MENU`) — โครงเปล่าก่อน (หน้า route มาทีหลัง)

### Validation
- เทียบสี/ระยะกับ `index.html` และ `reference/ui-target/`
- test responsive: mobile 375px ไม่มี overflow-x
- `npm run build`

### Design Reference
`index.html` (v1.15) — โครง shell, bottom-nav, ปุ่มขายลอย · `docs/04-design-system.md` §9 rules

### Progress (partial — In Progress)
เจอว่า token ใน FAM-1001 (จากสเปก §11) **ผิด** — ของจริง (docs/04 + index.html) คือ Yamaha red `#E60012`, ink `#16181D`, paper `#FAFAF8` + ระบบ 8-point ครบ → แทนที่แล้ว

ทำเสร็จรอบนี้:
- `app/globals.css` — design tokens จริง (docs/04 §2) map เป็น Tailwind theme (`bg-accent`,`text-ink`,`text-muted`,`border-hairline`,`font-display`,…) + `.tabular` + reduced-motion
- **ฟอนต์ self-host จริง** — Noto Sans Thai (vendored `app/fonts/noto-thai-{400,600,700}.woff2` จาก `tools/manual/fonts`) via `next/font/local` + Inter via `next/font` (self-host ตอน build, ไม่มี request Google ตอน runtime)
- `lib/nav/menu.ts` — port MENU จริง (20 หน้า × 6 กลุ่ม, role-gated) + `visibleMenu(roles)` + test 4 เคส
- อัปเดต `page.tsx`/`ConnectionBadge` เป็น token ใหม่

Divergence ที่ต้องให้ผู้ใช้ยืนยัน: docs/04 *blueprint* เขียนฟอนต์ Trirong/Anuphan แต่ **shipped index.html ใช้ Noto Sans Thai** (และ repo vendor เฉพาะ noto woff2) → เลือก shipped(Noto) ไว้ก่อน

เหลือ (รอบถัดไป): app shell components — sidebar(เดสก์ท็อป) + bottom-nav ตาม role + ปุ่มขายลอย(v1.2) + แผ่น "อื่นๆ" + topbar + placeholder routes 20 หน้า · ห้ามมี horizontal scroll ≤390px

Validation รอบนี้: `tsc` ✅ · `eslint` ✅ · `vitest` ✅ 14/14 · `next build` ✅ (ทั้งสองฟอนต์ self-host สำเร็จ)
