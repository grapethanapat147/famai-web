## FAM-1001: Scaffold Next.js + Supabase + envs + CI

Status: In Progress
Priority: High
Epic: FAM-E01
Refs: docs/02-architecture.md · docs/06-supabase-setup.md · .env.example

### Summary
ตั้งโครง Next.js (App Router, TypeScript, Tailwind) ในรีโปนี้โดยไม่แตะ `index.html`/`supabase`/`docs` เดิม, ต่อ Supabase project จริง (`hpsmjavfvrdctclmlmhp`) ผ่าน env, แยก client (publishable) / server (service_role), และตั้ง CI (lint+typecheck+build+test) + config Vercel

### Acceptance Criteria
- `npm run dev` รันได้, หน้าเปล่า `/` โหลดผ่าน (ใช้ design token base)
- โครง: `app/`, `lib/supabase/{browser,server}.ts`, `lib/settings/`, `lib/auth/`, `components/`, `tests/`
- `.env.local` อ่านจาก `.env.example` — `SUPABASE_SERVICE_ROLE_KEY`/`DATABASE_URL` ใช้ฝั่ง server เท่านั้น, ไม่ leak เข้า client bundle (มี test/ตรวจ)
- Tailwind config map design tokens §04 (`--red #D92D20`, `--ink #101828`, ฯลฯ) เป็น theme
- CI (GitHub Actions หรือ script): `lint`, `typecheck`, `build`, `test` — ผ่านทั้งหมด
- `vercel.json` เดิม (X-Robots noindex) ยังอยู่; เพิ่ม config Next.js ที่จำเป็นเท่านั้น
- ไม่ทำ `node tools/qa/run.js` (ต้นแบบ index.html) พัง

### Validation
- `npm run build && npm run lint && npm run test`
- grep bundle ยืนยันไม่มี service_role key ฝั่ง client
- โหลด `/` ด้วย publishable key + RLS ได้ (ไม่มี error console)

### Notes
- ยึด `docs/06-supabase-setup.md` เรื่องค่า env จริง · ห้าม commit `.env.local`
- ยังไม่ push (origin = repo เพื่อน) จนกว่าผู้ใช้ยืนยัน remote

### Implementation Notes (ready for review)
Stack: Next.js 16.3 (App Router, Turbopack) · React 19.2 · Tailwind 4 (CSS-first `@theme`) · TypeScript 5 · @supabase/ssr 0.12 + supabase-js 2.112 · Vitest 4

Files added (ทั้งหมด additive — ไม่แตะ index.html/docs/supabase/tools; แก้แค่ `.gitignore`):
- `app/{layout,page,globals.css}` — shell ภาษาไทย + design tokens §11.1 (8 สี) map เป็น Tailwind theme (`bg-brand`,`text-ink`,`border-line`,…) + Thai font stack (noloop default; web font โฮสต์เองใน FAM-1002)
- `lib/supabase/{browser,server,admin}.ts` — browser (publishable+RLS) / server (cookies, ผ่าน RLS) / admin (service_role) โดย server+admin มี `import "server-only"` guard
- `lib/settings/index.ts`, `lib/auth/index.ts` — stub ชี้ไป FAM-1004 / FAM-1005
- `components/ConnectionBadge.tsx` — client health check ยิง `pub.model` (anon 200)
- `tests/` (Vitest) — secret ไม่หลุด client + การแยกชั้น client/server client
- `.github/workflows/ci.yml` — lint+typecheck+test+build · `.env.local` (public keys, gitignored) · `next.config.ts` (turbopack root)

Validation (ผ่านทั้งหมด):
- `npm run typecheck` ✅  ·  `npm run lint` ✅ (scope เฉพาะ app; ignore tools/prototype/docs/supabase)
- `npm run test` ✅ 4/4  ·  `npm run build` ✅ (`/` prerendered static, ไม่มี warning)
- runtime: `next start` → `GET /` = **200** แสดง brand + หัวข้อไทย
- ไม่กระทบ QA เดิม: `node tools/qa/run.js --list` ยังลิสต์ครบ 33 ชุด · service_role ไม่อยู่ใน client bundle (test ยืนยัน)

ยังไม่ commit/push (รอผู้ใช้ยืนยัน remote) · session refresh middleware ยกไป FAM-1005
