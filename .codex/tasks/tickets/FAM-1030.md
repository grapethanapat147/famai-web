# FAM-1030 · อนุมัติค่าใช้จ่าย (expense approval)

**Epic:** FAM-E10 (บัญชีและพนักงาน)
**สถานะ:** Done (PR feat/expense-approve) — migration 19 apply แล้ว (own Supabase)
**ขึ้นกับ:** FAM-1019 (หน้าค่าใช้จ่าย list + บันทึก · ยังไม่มีอนุมัติ) · migration 19 (expense.approved_by/approved_at)

## เป้าหมาย
R1 §ค่าใช้จ่าย: "การเงินกดอนุมัติ ถูกต้อง/ผ่าน" — ให้คนมีสิทธิ์ approve กดอนุมัติรายการค่าใช้จ่าย

## ขอบเขต
- ด่านสิทธิ์ **perms.approve** (ไม่ใช่แค่ role) — admin/manager มี approve
- action `approveExpense` (CAS `approved_at IS NULL` กันอนุมัติซ้ำ) + `revokeExpenseApproval` (ถอนอนุมัติเผื่อพลาด)
- แสดงสถานะ "อนุมัติแล้ว / รออนุมัติ" + KPI ยอดรออนุมัติ + ปุ่มอนุมัติในรายละเอียด
- verify ผ่าน /dev/expense (mock) + ของจริงได้แล้ว (login live)

## หมายเหตุ
- คอลัมน์ approved_by/approved_at มาจาก migration 19 (apply แล้วบน own Supabase `xpbdvhfvmpokdnfhoujx`)
- ต้องเพิ่ม approved_by/approved_at ใน database.types (migration 19 ยังไม่ได้ sync type)
