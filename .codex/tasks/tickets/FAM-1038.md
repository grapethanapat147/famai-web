# FAM-1038 · หน้าตั้งค่าธีม (แอดมิน) (ticket 2/3 · UI/ธีม track)

**Epic:** FAM-E13 (UI/ธีม)
**สถานะ:** Done (PR feat/theme-settings)
**ขึ้นกับ:** FAM-1037 (theme engine foundation — ThemeStyle ฉีดสีเน้นจาก app_setting)
**Spec:** `.codex/specs/2026-08-17-theme-engine-design.md` §7-8

## เป้าหมาย
หน้าตั้งค่าธีมของร้าน (admin) — เลือก preset / ปรับสีเน้นเอง + พรีวิวสด → **เขียน `theme_accent`** (ปลดล็อกให้ engine FAM-1037 เปลี่ยนสีทั้งแอปจริง)

## ทำอะไร (Done)
- `lib/theme/presets.ts` — `THEME_PRESETS` (6: แดงยามาฮ่า/น้ำเงิน/เขียว/ส้ม/ม่วง/กราไฟต์) + `findPresetByAccent` (pure) + tests (+4)
- `app/(app)/settings/actions.ts` — `updateThemeSettings` (gate `perms.admin` + validate hex กัน CSS injection + upsert theme_accent + `revalidatePath("/","layout")` → ThemeStyle ฉีดใหม่ทั้งแอป)
- `components/theme/ThemeSettings.tsx` (client) — preset grid + สีเอง (color input + hex) + **พรีวิวสด** (ใช้ deriveAccent ตั้ง CSS var ในกรอบพรีวิว → ปุ่ม/badge/ลิงก์เปลี่ยนสีทันที) + ปุ่มบันทึก (dirty-gated)
- wire `settings/page.tsx` (getThemeConfig → ThemeSettings ใต้ SettingsView) + `dev/settings` (mock)

## Minor polish (จาก final review FAM-1037)
- DRY `DEFAULT_ACCENT` → export จุดเดียวจาก `derive.ts` (config.ts/ThemeStyle/presets ใช้ร่วม)
- `getThemeConfig` wrap ด้วย `React.cache` (memoize ต่อ request)
- theme toggle บน TopBar เพิ่ม `aria-pressed`

## Verify (browser /dev/settings)
- 6 preset render · กดน้ำเงินเข้ม → hex #1B49D6 + พรีวิว (ปุ่ม/ลิงก์) เป็นน้ำเงิน rgb(27,73,214) ทันที
- custom hex ผิด (`notahex`) → เตือน "รูปแบบต้องเป็น #RRGGBB" + ปุ่มบันทึก disabled
- กดบันทึก (mock) → "บันทึกธีมแล้ว" · mobile 375 ไม่มี h-scroll · console สะอาด
- tsc + eslint + build ผ่าน · vitest 219 ผ่าน
- **เปลี่ยนสีทั้งแอปจริง + admin gate = เกรพเทสตอน login** (updateThemeSettings เขียน DB + revalidate)

## ตามมา
- FAM-1039 ระบบฟอนต์ + อัปโหลด (curated pairs + bucket brand-font + @font-face)
