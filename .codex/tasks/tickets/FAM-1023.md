# FAM-1023 · sell_unit() RPC — บันทึกการขายจริง (atomic)

**Epic:** FAM-E06 (ขายรถ)
**สถานะ:** Done (PR feat/sell-real) — migration 20 apply แล้วบน own Supabase
**ขึ้นกับ:** FAM-1011 (หน้า /sell คิดดีล + ฟอร์ม · บันทึกลง DB ค้าง) · own Supabase + login (2026-08-17)

## เป้าหมาย
ปลดล็อกการขายจริง — หน้า /sell เดิม form-only (deferred รอ RPC) → บันทึกลง DB แบบ atomic

## ขอบเขต (Done)
- **migration 20 `sell_unit()` RPC** (PL/pgSQL, security definer):
  - unique index `sale_unit_active_uniq` (unit_id where voided_at is null) — กันบิลซ้ำต่อคันระดับ DB
  - ล็อกคัน `for update` + เช็ค status='available' (กันขายซ้ำแม้เปิดพร้อมกัน)
  - ด่านสิทธิ์ในตัว: role ขาย (admin/manager/sales) + สาขา (is_all_branch/my_branches) — เพราะ security definer ข้าม RLS
  - คิดต้นทุน/กำไรฝั่ง DB จากต้นทุนจริงของรถ + `freebie_is_cost` setting (ไม่เชื่อ client)
  - สร้าง: ลูกค้า walk-in → บิลขาย (doc_no TAXINV) → set unit sold → **registration (§9f)** → finance_case (ถ้าเงินผ่อน) → follow_up_task (จาก follow_up_cadence)
  - return jsonb {sale_id, doc_no, customer_id}
- **lib/rpc `sellUnit()` wrapper** + database.types Functions.sell_unit
- **lib/sell/sell.ts `canSell()`** (admin/manager/sales) + SellActionResult + test
- **action `recordSale`** (gate canSell + validate + เรียก RPC + revalidate /sell,/stock,/deal)
- **SellForm**: เพิ่มช่องลูกค้า (ชื่อ*/เบอร์) + wire ConfirmDialog → action + success(doc_no)/error + reset กันบันทึกซ้ำ · preview mode เมื่อไม่มี action
- **sell/page**: ส่ง action + sellerBranchCode จากสาขาผู้ใช้ · dev/sell: mock

## Verify
- migration 20 applied remote (local===remote) · smoke test: anon เรียก sell_unit → 42501 (มีฟังก์ชัน + ป้องกัน)
- /dev/sell: เลือกคัน+ลูกค้า → ยืนยัน → success "เลขที่ FMG-TAXINV-2569-00042" + ฟอร์มรีเซ็ต · mobile 375 ไม่มี h-scroll · console สะอาด
- tsc+eslint+build ผ่าน · vitest 201 ผ่าน
- **ทดสอบขายจริง (login) = เกรพทำเอง** (สร้างบิล→เช็ค deal/stock)

## ตามมา (ยังไม่ทำ)
- แปลงใบเสนอราคา→การขาย (เรียก recordSale) · ดอกเบี้ย tiered ตอนขาย · เลือกลูกค้าเดิม (ตอนนี้สร้าง walk-in ใหม่เสมอ) · sale_freebie รายชิ้น (ตอนนี้เก็บ freebie_cost รวม)
