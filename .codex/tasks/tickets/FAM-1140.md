# FAM-1140 — sync กระดานงาน + กันเลข migration ใน README ค้างอีก

**สถานะ:** Done
**สาขา (git):** `chore/FAM-1140-board-sync`
**ต้องรัน SQL:** ไม่ต้อง

## ปัญหา
กระดานงาน sync ครั้งสุดท้ายตอน FAM-1130 (3 ก.ย.) หลังจากนั้นมีตั๋วเข้า main อีก 10 ใบ
(FAM-1130 ถึง FAM-1139) ที่ยังไม่ขึ้นกระดาน — `done.md` เขียน 126 ใบ ทั้งที่ของจริง 136

เจอปัญหาย่อยอีกสามอย่างตอนไล่ประกอบกระดานกลับจาก `git log`:

1. **FAM-1135 ไม่มีไฟล์ตั๋ว** — งานเข้า main แล้ว (PR #137) แต่ตอนนั้นไม่ได้สร้างไฟล์ไว้
   เป็นใบเดียวใน main ที่ประกอบกระดานกลับไม่ได้
2. **ตัวเลข migration ใน README ค้างอีกรอบ** — FAM-1135 แก้ให้เป็น 36/37 แล้วค้างทันทีที่
   migration 38 เข้ามาใน FAM-1137 (ของจริงตอนนี้ 37 ไฟล์ · ล่าสุด 38)
3. **สาขา `chore/security-hardening-ticket` ค้างอยู่** ถือตั๋วความปลอดภัยที่ยังใช้ได้อยู่ใบหนึ่ง
   แต่แตกจาก main รุ่นเก่ามาก — merge ตรง ๆ จะลบตั๋วบนกระดานทิ้งกว่า 130 ใบ

## ทำอะไรบ้าง

**ประกอบ `done.md` กลับจาก `git log main`** — 126 → **136 ใบ** แถวเดิมคงข้อความไว้ทุกตัว
เพิ่มเฉพาะ 10 ใบใหม่ (ดึงหัวข้อจากไฟล์ตั๋วเอง) · อัปเดตวันที่ sync ของ `todo` / `in-progress` / `review` / `backlog`

**เขียนตั๋ว FAM-1135 ย้อนหลัง** ให้ครบตามธรรมเนียม ticket-first

**`tests/docs-migrations.test.ts` (3 เคส)** — ผูกตัวเลขใน README เข้ากับโฟลเดอร์ `supabase/migrations/` จริง
จำนวนไฟล์และหมายเลขล่าสุดไม่ตรงเมื่อไหร่เทสต์แดงทันที บวกเคสกันเลขลำดับ migration ซ้ำกัน
(กรณีสองสาขาเผลอใช้เลขเดียวกัน) — จะได้ไม่ต้องมีตั๋วแบบ FAM-1135 อีก · แก้ README เป็น 37 ไฟล์ (ล่าสุด 38)

**ยกตั๋วความปลอดภัยออกจากสาขาค้าง** มาเป็น `FAM-1141` ใน backlog บน main
บนสาขานั้นตั๋วใช้เลข FAM-1021 ซึ่ง main ใช้ไปแล้วกับ "หน้ารายงาน" (คนละใบ เลขชนกัน) จึงให้เลขใหม่
เนื้อหาปรับให้ตรงปัจจุบัน: เพิ่มคอลัมน์เงินที่เกิดทีหลัง (`wholesale_order_line.cost`,
`part_movement.unit_cost`, `payslip`) และตัดเงื่อนไข "รอผลตัดสิน บริษัท vs สาขา" ที่จบไปแล้วใน FAM-1111/1129

**`backlog.md`** จากว่างเปล่าเป็นมี FAM-1141 · `todo.md` ชี้ทางไป backlog และบอกว่างานที่เจ้าของต้องกดเอง
อยู่ในหัวข้อ "ก่อนเปิดใช้จริง" ของ README ไม่ใช่ตั๋วบนกระดาน

## ที่เจ้าของต้องกดเอง
ลบสาขา `chore/security-hardening-ticket` บน GitHub ได้เลย — เนื้อตั๋วยกมาไว้ที่ FAM-1141 แล้ว
สาขานั้นไม่มีโค้ด มีแต่ไฟล์กระดานรุ่นเก่าที่ merge ไม่ได้

## ตรวจแล้ว
639 เทสผ่าน · typecheck · lint · build
