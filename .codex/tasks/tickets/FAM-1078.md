# FAM-1078 · ฟอร์มแก้ข้อมูลกิจการ/สาขา (บนหัวเอกสาร)

**Epic:** FAM-E07 (Settings) · **สถานะ:** In Progress (branch feat/company-info)
**ที่มา:** เกรพ — "ทำส่วนที่จำเป็นอย่างสุดท้าย" · ชิ้นสุดท้ายให้ระบบเอกสารสมบูรณ์

## เป้าหมาย
เอกสารทุกใบ (ใบเสนอราคา/ใบขาย/ใบสั่งซ่อม/สลิป/**ใบกำกับภาษี**) ดึงหัวจาก branch (fallback company) — แต่ seed ใส่แค่ชื่อ → เลขภาษี/ที่อยู่โชว์ "—" · ทำ **ฟอร์ม admin แก้ข้อมูลกิจการ/สาขา** ในหน้า settings (เดิมแก้ได้ทาง SQL เท่านั้น)

## บริบท (FAM-1022 = ทำเสร็จแล้วในโค้ด)
migration 16 seed **"1 บริษัท (FAMAI) ครอบ 3 สาขา"** ตรงกับคำตอบเกรพ → ไม่ต้องแก้ schema · งานนี้แค่เติมช่องกรอกข้อมูลใน UI

## ทำอะไร
- **`lib/org/info.ts`** (ใหม่, pure) — types (OrgCompany/OrgBranch/OrgInfoActionResult) + `parseTaxId` (ว่าง=null · กรอก=ต้อง 13 หลัก) + `nullIfBlank` + tests
- **`components/settings/CompanyInfoView.tsx`** (ใหม่, client) — การ์ดในหน้า settings: บริษัท (1) + สาขา (N) แก้ ชื่อ/เลขภาษี/ที่อยู่/เบอร์ · แก้ได้เฉพาะ admin · saved/error badge · a11y label
- **`app/(app)/settings/actions.ts`** — `updateOrgInfo`: admin gate → validate → `update company` (RLS company_admin) + `update branch` (RLS ปิด, ใช้ grant) · revalidate
- **`app/(app)/settings/page.tsx`** — ดึง company+branches ส่งเข้า CompanyInfoView
- **`app/dev/settings/page.tsx`** — พรีวิว mock

## Verify
- build + tsc + eslint + vitest (+3) เขียว
- /dev/settings → การ์ด "ข้อมูลกิจการ/สาขา" มีบริษัท + 3 สาขา · กรอกเลขภาษีผิด (ไม่ครบ 13) → เตือน · ถูก → บันทึกแล้ว (mock)
- จริง: admin เติมเลขภาษี/ที่อยู่ → เอกสารทุกใบขึ้นครบ (เกรพเทสตอน login)
