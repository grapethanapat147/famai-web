# FAM-1041 · UI polish รอบ craft & interaction (shared primitives)

**Epic:** FAM-E13 (UI/ธีม track — งานที่ 5, ปิด track)
**สถานะ:** Done (branch feat/ui-polish)
**ที่มา:** วิชันเจ้าของ — "ปรับ UI ให้สวยงามขึ้น practical ขึ้น ยืดหยุ่นขึ้น"

## แนวคิด
design system (docs/04) โตแล้วและมีวินัยดี (8-point, ink-tinted shadow, tabular-nums, แดง=ช่วงเวลา, dark+density)
→ งานคือ **ขัดความประณีต ไม่ใช่รื้อ** · แก้ที่ primitives ที่ใช้ทั้งแอป → propagate ทุกหน้า
(ตัวเลข StatCard ที่ "โล่ง" เป็นเพราะไม่มีข้อมูลช่วงก่อนจริง — ไม่ใส่ trend ปลอม)

## ทำอะไร (Done)
- **globals.css** — เพิ่ม `:focus-visible` ring สีเน้นเดียวทั้งแอป (a11y + craft; เดิมไม่มี focus state) เฉพาะคีย์บอร์ด ไม่ขึ้นตอนคลิกเมาส์ · ไม่รวมช่องกรอก (มีสไตล์เอง)
- **HBarChart** — แก้บั๊ก tone `info` ที่ map เป็น `bg-info` (ไม่มีสีนี้ → แท่งหาย) → `bg-ink-soft` ให้ตรง StatusBadge · label กว้างขึ้น (w-28→w-32) + `title` เต็มตอน hover · แท่งมี transition ตอนเปลี่ยนค่า
- **DataTable** — แถวเดสก์ท็อป: transition + `active:` กด + ring โฟกัสคีย์บอร์ดแบบ inset · การ์ดมือถือ: `active:scale` ตอนกด
- **Chips** — กดมีสปริง (`active:scale-95`) + hover เห็นขอบ
- **StatCard** — เพิ่ม `tone="accent"` (วงแหวน+ตัวเลขสีเน้น) → dashboard "ยอดค้างจ่าย" ที่เดิม hard-code มาใช้ primitive (DRY, หน้าตาเดิมเป๊ะ)

## Verify (browser, dev routes)
- focus ring: Tab → ชิป "พร้อมขาย" ได้ `outline 2px rgb(230,0,18)` + `:focus-visible` true · ตามรูปทรง pill
- table: hover แถว → bg `rgb(244,243,239)` (paper-2) ✓
- dashboard: accent card เหมือนเดิม · label กราฟโชว์ "Famai Motor Group" เต็ม
- dark mode: ring/hover/tokens ปรับตามธีม ✓ · mobile 375 ไม่มี h-scroll · console สะอาด (เหลือแต่ HMR ws ของ dev)
- tsc + eslint + vitest (233) + build ผ่าน

## ปิด track UI/ธีม
FAM-1037 engine · 1038 สี · 1039 ฟอนต์ · 1040 แคปโหมดลูกค้า · 1041 craft polish — ครบ
