# FAM-1048 · UI polish — เจาะหน้ารายงาน (ReportView)

**Epic:** FAM-E13 (UI/ธีม track — polish รอบ 7)
**สถานะ:** In Progress (branch feat/report-polish)
**ที่มา:** เกรพ — "ทำต่อในลำดับที่เหมาะสม"

## ทำไมเลือก Report
- front-of-house สายขายเจาะครบ (Dashboard/Sell/Quote) · **Service ขัดดีอยู่แล้ว** (status chips คลิกกรองได้+โชว์ active, qty โชว์, ใช้ Chips component) headroom น้อย
- **Report มี headroom จริง** — เลขพาดหัว (ยอดขายรวม/กำไร/ยอดค้าง) ถูกซ่อนในแถว "รวม" ท้ายตาราง · เป็น management surface คู่กับ Dashboard ที่เจาะไปแล้ว

## ทำอะไร (aggregate logic เดิม ไม่แตะ)
- **แถวพาดหัว (hero totals)** — ยก totals ขึ้นเป็น StatCard เหนือตาราง: จำนวน + คอลัมน์ยอดรวมแต่ละอัน (ยอดขาย → 3 ใบ: จำนวน/ยอดสุทธิ/กำไร · ค่าใช้จ่าย/AR → 2 ใบ) · ผู้บริหารเห็นเลขพาดหัวทันทีไม่ต้องกวาดไปท้ายตาราง
- **AR เน้นสีแดง** — "ยอดค้าง" ใช้ tone="accent" (เงินค้างรับ = ต้องจับตา) · ยอดขาย/ค่าใช้จ่าย = ปกติ (กันแดงบนรายได้ที่ดู negative)
- **แถวตารางมี hover** (transition) · แถว "รวม" ท้ายตารางคงไว้ (มาตรฐาน + พิมพ์; hero เป็น print:hidden ไม่ชนกัน)

## Verify (/dev/report)
- ยอดขาย → hero 3 ใบ: 6 รายการ · ยอดสุทธิ 542,800฿ · กำไร 61,900฿
- เงินค้างรับ → hero 2 ใบ: 3 รายการ · ยอดค้าง 280,900฿ (แดง+ring) · ไม่มี date filter
- mobile 375 ไม่มี page h-scroll (การ์ด stack, ตารางเลื่อนในตัว) · dark สะอาด · console สะอาด
- tsc + eslint + vitest (235) + build ผ่าน
