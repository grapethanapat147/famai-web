# FAM-1097 · sort/filter หน้าที่ยังไม่มี

**ประเภท:** (enhancement) · **ที่มา:** เกรพ (batch งานเสริม)

## สำรวจ
หน้ารายการเกือบทั้งหมดมี filter/ค้นหาแล้ว (สต๊อก · service · parts · expense · ar · deal · quote · transfer · attend · report · registration · acct)
→ **ขาดแค่ /models (แคตตาล็อกรุ่น)** ที่ยังไม่มีค้นหา/กรอง/เรียง

## ทำ ✅ (branch feature/FAM-1097-models-filter) — ไม่ต้อง migration
- /models: เพิ่มแถบ **ค้นหา** (ชื่อ/รหัส/สี) + **กรองประเภท** (chips อัตโนมัติจากข้อมูล) + **เรียง** (ชื่อ/ราคา↑↓/คงเหลือ)
- empty state เมื่อกรองแล้วไม่เจอ + ปุ่มล้างตัวกรอง · ใช้ได้ทั้งมุมมองการ์ด+ตาราง

## โครงสร้าง/เทส
- `lib/models/rows.ts`: +filterModels · sortModels · modelCategories (pure)
- ModelsView: +search/category/sort state (useMemo) + FilterBar
- tests/models.test.ts +3 describe (filter/sort/categories) · สวีทเต็ม 422 ผ่าน
- typecheck · lint · build เขียว · (preview /dev/models) กด Moped → เหลือ FINN (1 รุ่น) ✓
