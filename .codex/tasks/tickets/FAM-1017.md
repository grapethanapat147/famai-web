## FAM-1017: Dashboard (ภาพรวม)

Status: Done
Priority: High
Epic: FAM-E07
Refs: Spec §9.1, §6.7 · .codex/specs/revision-1-client-feedback.md (Dashboard: ยอดค้างจ่ายตัวแดงเด่น)

### Summary
หน้า `/dash` — KPI + กราฟจากข้อมูลจริง (RLS) เน้นฝั่งสต๊อก (ข้อมูลที่มี 50 คัน) + **ยอดค้างจ่าย (R1: ตัวแดง เด่น)** จาก `receivable.balance`; money-strip ค่าที่เป็นเงิน

### Acceptance Criteria
- `/dash` ดึง `motorcycle_unit` (RLS) + `receivable` (ยอดค้าง) + `sale` (นับเดือนนี้) + settings (aging_days/buckets)
- KPI: สต๊อกคงเหลือ · มูลค่าสต๊อก (money) · รถค้างเกินเกณฑ์ · ทุนจมรถค้าง (money) · ขายเดือนนี้ (compare — ช่วงก่อนไม่มีข้อมูลถ้าไม่มี, §6.7) · **ยอดค้างจ่าย (แดง เด่น)**
- กราฟ (SVG/div เอง ไม่ใช้ lib — docs/04 §10): สต๊อกแยกตามสาขา · ช่วงอายุสต๊อก (bucket, แท่งเกินเกณฑ์เป็นแดง)
- ตัวกรองสาขา · money-strip: ไม่มีสิทธิ์ `money` → ค่าเงินเป็น '—' (ไม่ส่งไป client)
- มือถือไม่มี h-scroll · ตัวเลข KPI ไม่ตกบรรทัด

### Validation
- `tsc`/`eslint`/`vitest` (test `stockStats`)/`next build` · preview `/dev/dash`
- (ข้อมูลจริง + RLS + role ตรวจเมื่อมี test user)

### Out of scope
- กราฟยอดขายตามช่วงเวลา (รอมีข้อมูลขาย) · ผลงานเซลล์ · ซ่อน/แสดงการ์ด · เทียบช่วงเต็มรูป (§6.7 กับ resolution auto)

### Implementation Notes (ready for review)
Files:
- `lib/dashboard/stats.ts` (pure) — `stockStats(units,{agingDays,buckets,canSeeMoney})` → in-stock/aged counts, มูลค่า/ทุนจม (null ถ้าไม่มีสิทธิ์), byBranch, aging buckets (แท่งสุดท้าย tone bad) + tests
- `components/ui/HBarChart.tsx` — กราฟแท่งแนวนอน (div, ไม่พึ่ง lib — docs/04 §10) แท่งหมึก, แดงเฉพาะเกินเกณฑ์
- `components/dashboard/DashboardView.tsx` (client) — ตัวกรองสาขา + KPI 6 การ์ด + **ยอดค้างจ่าย (ตัวแดง + red ring, R1)** + 2 กราฟ
- `app/(app)/dash/page.tsx` (server) — motorcycle_unit + receivable(balance,settled_at) + sale(sold_at เดือนนี้) + settings ผ่าน RLS · **stripMoneyFields cost** ก่อนส่ง client · overdue = sum(balance where ยังไม่ settle) money-guarded
- `app/dev/dash/page.tsx` preview · เพิ่ม `sale`+`receivable` เข้า Database types

Validation: `tsc`/`eslint`/`vitest` (38/38)/`next build` (28 หน้า) ✅
**ตรวจ preview:** เดสก์ท็อป KPI+ยอดค้างจ่ายแดงเด่น + กราฟสาขา/อายุ (เลขถูก: สต๊อก 9, มูลค่า 739,800, ค้าง 3, buckets 3/1/2/3) · มือถือ KPI 2-col **ไม่มี h-scroll** · `/dash` noauth = 307→/login
⏳ ข้อมูลจริง + RLS/money ต่อ role ตรวจเมื่อมี test user
