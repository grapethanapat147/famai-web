# FAM-1093 · ยกเครื่องหน้า deal (epic — ทำเป็น phase)

**Epic:** (feature) · **ที่มา:** เกรพ (รูป4 + คำสั่งเพิ่มหลายรอบ)

## Phase 1 ✅ (branch feature/FAM-1093-deal-modal) — modal กลางจอ + แก้ข้อมูลลูกค้า + draft + เลขถัง
- drawer ขวา → **modal กลางจอ** (Modal size="lg")
- **เลขถัง (frame_no)** โชว์ในดีล + เป็นคีย์เชื่อมข้อมูล (โยงมากสุดตามที่เกรพขอ)
- section **"ข้อมูลลูกค้า / บันทึก"** แก้ได้: ชื่อ/เบอร์/ที่อยู่(ตามบัตร)/เลขบัตรประชาชน(13หลัก)/หมายเหตุ
  - `updateDealCustomer` → update customer + registration.note (ด่านสิทธิ์ deal, RLS สาขา)
- **จำ draft** ใน localStorage อัตโนมัติ → เผลอปิด/ย้อนกลับมาแก้ต่อได้ (badge "ร่างยังไม่บันทึก" + เปิดให้อัตโนมัติ)
- pure `validateDealCustomer` + test · Modal เพิ่ม prop size

## Phase ต่อไป (ยังไม่ทำ)
- **P2** กดแต่ละ step เปิดฟอร์มใส่ข้อมูล/สถานะย่อย (เช่น "คุยกับลูกค้า" → คุยอยู่/หาย/กำลังตัดสินใจ) + เวลาอัปเดตต่อช่อง → ต้อง migration (per-step data)
- **P3** ไฟแนนซ์ย่อย: ส่งเรื่อง→รอผล→ผ่าน(เปิดการขาย)/ไม่ผ่าน(ระบุเหตุผล จบ)
- **P4** ขั้นส่งมอบ: วันที่/สถานที่/ผู้ส่งมอบ → migration
- เชื่อมกับ FAM-1100 (หน้าทะเบียน — ป้ายจริง 2 สเตจ)

## Verify (P1)
- unit (validateDealCustomer) + สวีท 356 · typecheck · lint · build เขียว
- (preview /dev/deal) เปิดดีล = modal กลางจอ · โชว์เลขถัง · แก้ข้อมูลลูกค้า · พิมพ์แล้วปิด→เปิดใหม่ draft ยังอยู่
