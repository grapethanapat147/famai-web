# FAM-1070 · Pre-launch QA sweep + sync task board/docs

**Epic:** (housekeeping) · **สถานะ:** In Progress (branch chore/docs-sync-qa)
**ที่มา:** เกรพ — "ไปงานต่อได้เลย" หลัง build ครบทุก epic · roadmap จบ เลยทำ QA sweep + เก็บสถานะจริง

## เป้าหมาย
1. กวาด QA ก่อน launch เท่าที่ทำได้โดยไม่ต้อง login (dev preview)
2. sync เอกสาร tracking ที่ล้าสมัยหนัก (backlog/in-progress/UNBLOCK ยังเขียนว่า "9 commit บน feat/nextjs-app / push to GrapeThanapat") ให้ตรงความจริง

## QA sweep — ผล (ผ่าน ✅)
- **22 `/dev/*` routes ตอบ 200 หมด** ไม่มี 500/crash
- **server log สะอาด** — ไม่มี error/warn/hydration ตอน render ทั้ง 22 หน้า
- **mobile 375px ไม่มี h-scroll** — ตรวจหน้าเสี่ยงสุด (ตารางกว้าง/กริดรูป): stock · report · payroll · models · parts · settings → `scrollWidth == innerWidth` ทุกหน้า (shared DataTable/layout จัดการ overflow ดีอยู่แล้ว)
- **console errors = noise ของ dev เท่านั้น**: HMR WebSocket ผ่าน preview-proxy (ไม่มีใน prod build) + 401 เก่า 3 อันจาก `/api/ai/*` ที่ยิงตอนเทสก่อนถอด AI UI (timestamp ก่อน sweep · UI ที่ trigger ถูกลบไปแล้ว)
- **ไม่พบบั๊กที่ต้องแก้** — ไม่มี code change ในรอบนี้ (docs อย่างเดียว)

## Sync docs
- `.codex/tasks/backlog.md` — เขียนใหม่: epic ครบ E01–E12, E12 dormant (FAM-1069), เหลือ FAM-1022 (blocked), pre-launch checklist
- `.codex/tasks/in-progress.md` — เคลียร์ list เดิม (merge หมด) → ชี้ FAM-1022 + backlog
- `.codex/UNBLOCK.md` — §1 push = ✅ done (repo grapethanapat147/famai-web), แก้ status line ให้ตรง

## ทำไม login ยังทดสอบเองไม่ได้
route จริง (`/dash` ฯลฯ) ต้อง session — ผมกรอกรหัสผ่านแทนไม่ได้ · flow จริง + RLS/money-strip = เกรพเทสตอน login (UNBLOCK §2)
