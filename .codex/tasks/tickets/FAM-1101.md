# FAM-1101 · ลงเวลาแบบตรวจพิกัด (geofence) — P1

**ประเภท:** (feature) · **ที่มา:** เกรพ (เลือกจากงานเสริม) — กันลงเวลาไกลร้าน

## ⚠️ ต้องรัน migration ก่อน merge
`supabase/migrations/20260823160000_26_attendance_geofence.sql` (idempotent · RLS เดิมครอบคลุม):
- `branch`: geo_lat, geo_lng (double precision), geo_radius_m (int) — จุดร้าน + รัศมี (ว่าง = ปิด geofence)
- `attendance`: check_in_lat, check_in_lng, check_in_distance_m — พิกัด+ระยะตอนลงเวลา

**เปิดใช้ต่อบริษัท** (ตั้งพิกัดร้านจริง — ว่างไว้ = ปิด, ลงเวลาได้ตามเดิม):
```sql
update branch set geo_lat = 13.75000, geo_lng = 100.50000, geo_radius_m = 150 where code = 'FMG01';
```

## ทำ ✅ (branch feature/FAM-1101-geofence) — P1: พิกัด (เซลฟี่ = P2)
- **opt-in ต่อบริษัท**: ตั้ง geofence แล้ว → ลงเวลาเข้าต้องส่งพิกัด + อยู่ในรัศมี · ไม่ตั้ง = ทำงานเหมือนเดิม (ไม่ต้อง GPS)
- clock-in: client ขอ GPS (navigator.geolocation) แนบ lat/lng → server เช็คระยะ (haversine) บล็อกถ้าเกินรัศมี + เก็บพิกัด/ระยะ
- การ์ดลงเวลา: ป้ายบอกรัศมี · ปุ่มขึ้น "กำลังหาตำแหน่ง…" · ข้อผิดพลาด GPS ชัดเจน

## โครงสร้าง/เทส
- `lib/hr/geo.ts` (pure): haversineMeters · withinGeofence · formatDistanceM · branchGeofence · validateGeoConfig
- `clockIn(formData)` (เดิมไม่รับ args) เช็ค geofence · `HrView` ClockCard ขอ GPS · page ส่ง geofence ของบริษัท
- database.types: +branch.geo_* +attendance.check_in_* · tests/geo.test.ts (12 เคส)
- สวีทเต็ม **401 ผ่าน** · typecheck · lint · build (/hr ƒ) เขียว
- (preview /dev/hr) ป้ายรัศมี 150 ม. แสดง · กดลงเวลา → ขอ GPS → (พรีวิวปิด GPS) error ถูกต้อง ✓
  - ⚠️ happy path (GPS จริงในรัศมี) ต้องเทสบนมือถือจริง — logic เช็คระยะ unit-tested ครบ

## P2 (ยังไม่ทำ)
- ถ่ายเซลฟี่ยืนยันตอนลงเวลา · ตั้งพิกัดร้านผ่านหน้า "ตั้งค่า" (ตอนนี้ตั้งผ่าน SQL) · HR แก้เวลาย้อนหลัง
