# FAM-1101 · ลงเวลาแบบตรวจพิกัด (geofence) — P1

**ประเภท:** (feature) · **ที่มา:** เกรพ (เลือกจากงานเสริม) — กันลงเวลาไกลร้าน

## ⚠️ ต้องรัน migration ก่อน merge
`supabase/migrations/20260823160000_26_attendance_geofence.sql` (idempotent · RLS เดิมครอบคลุม):
- `branch`: geo_lat, geo_lng (double precision), geo_radius_m (int) — จุดร้าน + รัศมี (ว่าง = ปิด geofence)
- `attendance`: check_in_lat, check_in_lng, check_in_distance_m — พิกัด+ระยะตอนลงเวลา

**เปิดใช้ต่อบริษัท** (ตั้งพิกัดร้านจริง — ว่างไว้ = ปิด, ลงเวลาได้ตามเดิม):
```sql
update branch set geo_lat = 13.75000, geo_lng = 100.50000, geo_radius_m = 150 where code = 'FMG01';
```

## ทำ ✅ (branch feature/FAM-1101-geofence) — P1: พิกัด (เซลฟี่ = P2)
- **opt-in ต่อบริษัท**: ตั้ง geofence แล้ว → ลงเวลาเข้าต้องส่งพิกัด + อยู่ในรัศมี · ไม่ตั้ง = ทำงานเหมือนเดิม (ไม่ต้อง GPS)
- clock-in: client ขอ GPS (navigator.geolocation) แนบ lat/lng → server เช็คระยะ (haversine) บล็อกถ้าเกินรัศมี + เก็บพิกัด/ระยะ
- การ์ดลงเวลา: ป้ายบอกรัศมี · ปุ่มขึ้น "กำลังหาตำแหน่ง…" · ข้อผิดพลาด GPS ชัดเจน

## โครงสร้าง/เทส
- `lib/hr/geo.ts` (pure): haversineMeters · withinGeofence · formatDistanceM · branchGeofence · validateGeoConfig
- `clockIn(formData)` (เดิมไม่รับ args) เช็ค geofence · `HrView` ClockCard ขอ GPS · page ส่ง geofence ของบริษัท
- database.types: +branch.geo_* +attendance.check_in_* · tests/geo.test.ts (12 เคส)
- สวีทเต็ม **401 ผ่าน** · typecheck · lint · build (/hr ƒ) เขียว
- (preview /dev/hr) ป้ายรัศมี 150 ม. แสดง · กดลงเวลา → ขอ GPS → (พรีวิวปิด GPS) error ถูกต้อง ✓
  - ⚠️ happy path (GPS จริงในรัศมี) ต้องเทสบนมือถือจริง — logic เช็คระยะ unit-tested ครบ

## P2 ✅ (branch feature/FAM-1101-p2-selfie) — ถ่ายเซลฟี่ยืนยัน
**ต้องรัน migration:** `20260823170000_27_attendance_selfie.sql`
- `branch.require_selfie` (bool, opt-in แยกจาก geofence) · `attendance.check_in_selfie` (path)
- bucket `attendance-selfie` **private** (public=false) + RLS อ่าน/เขียนเฉพาะผู้ล็อกอิน (ข้อมูลส่วนบุคคล)
- เปิดต่อบริษัท: `update branch set require_selfie = true where code = 'FMG01';`

- เปิด require_selfie → กดลงเวลาเข้าเปิดกล้องหน้า (capture="user") → ย่อ WebP (ลบ GPS ในรูป) → อัปขึ้น bucket private → แนบ path → server บันทึก
- flow เป็นเฟส: หาตำแหน่ง → อัปโหลดเซลฟี่ → ลงเวลา · server บังคับมีเซลฟี่เมื่อ require_selfie
- `lib/hr/selfie.ts` (pure: selfieObjectPath + const) + test · สวีทเต็ม 404 ผ่าน · build เขียว
- (preview /dev/hr) ป้าย 📍 + 🤳 แสดงคู่กัน · ⚠️ ถ่าย/อัปเซลฟี่จริงต้องเทสมือถือ (กล้อง+storage)

## P3a ✅ (branch feature/FAM-1101-p3-manage) — ตั้งค่า geofence/เซลฟี่ผ่าน UI (ไม่ต้อง SQL)
- หน้า **ตั้งค่าระบบ → ข้อมูลกิจการ/บริษัท** เพิ่มแผง "ลงเวลาเข้า — พิกัด + เซลฟี่" ต่อบริษัท
- ช่อง ละติจูด/ลองจิจูด/รัศมี + ปุ่ม **"ใช้ตำแหน่งปัจจุบัน"** (ดึง GPS เติมให้) + ป้ายเปิด/ปิด geofence สด
- checkbox **บังคับถ่ายเซลฟี่** · บันทึกผ่าน updateOrgInfo (validateGeoConfig เดิม) → เขียน branch.geo_*/require_selfie
- ว่างทั้ง 3 ช่อง = ปิด geofence · แอดมินตั้งเองได้ ไม่ต้องแตะ SQL อีก
- `lib/org/info.ts` OrgBranch +geo/selfie +parseCheckbox (test) · ไม่ต้อง migration (คอลัมน์มีจาก P1/P2)
- สวีทเต็ม 405 ผ่าน · typecheck · lint · build เขียว · (preview /dev/settings) 3 บริษัทแสดงแผงครบ ✓

## P3b ✅ (branch feature/FAM-1101-p3b-view-selfie) — HR ดูเซลฟี่ในหน้าเช็คการเข้างาน
- หน้า **ภาพรวมการเข้างาน** เพิ่มคอลัมน์ **เซลฟี่** (thumbnail กลม) + โชว์ **ระยะห่าง 📍{ระยะ}ม.** ในช่องเข้างาน
- กด thumbnail → โมดัลรูปใหญ่ + เวลาเข้างาน + ระยะห่างจากจุดร้าน
- bucket private → ออก **signed URL** ฝั่งเซิร์ฟเวอร์ (createSignedUrls ทีเดียวทั้งวัน, หมดอายุ 1 ชม.)
- `lib/hr/selfie.ts` +buildSignedMap (test) · AttendRow +selfieUrl/distanceM · ไม่ต้อง migration
- สวีทเต็ม 406 ผ่าน · typecheck · lint · build เขียว · (preview /dev/attend) thumbnail+ระยะ+โมดัลครบ ✓

## P4 ✅ (branch feature/FAM-1101-p4-edit-time) — HR แก้เวลาเข้า/ออกย้อนหลัง
- หน้าภาพรวมการเข้างาน: ปุ่ม **"แก้เวลา"** ต่อแถว (เฉพาะ admin/manager/hr) → โมดัลแก้เวลาเข้า/ออก
- prefill เวลาเดิม (ISO → HH:MM ไทย) · เวลาออกว่าง = ยังไม่ออก · ระบบคำนวณ สาย/ชั่วโมงงาน/สถานะใหม่
- `editAttendance` upsert (ไม่มีแถว = สร้างให้) · timestamptz +07:00 · ด่านสิทธิ์ canEditAttendance
- `lib/attend/attendance.ts` +canEditAttendance/bangkokTimestamp/validateAttendanceEdit (pure, test) · AttendRow +checkOut
- ไม่ต้อง migration · สวีทเต็ม 414 ผ่าน · typecheck · lint · build เขียว
- (preview /dev/attend) ปุ่มแก้เวลาทุกแถว → โมดัล prefill 08:20 + คำนวณใหม่ ✓

**epic FAM-1101 ครบ: P1 geofence · P2 เซลฟี่ · P3a ตั้งค่า UI · P3b ดูเซลฟี่ · P4 แก้เวลา** ✅
