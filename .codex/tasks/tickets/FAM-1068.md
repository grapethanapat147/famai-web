# FAM-1068 · E12 Phase 3 — AI ช่วยจับคู่คอลัมน์ตอนนำเข้า .xls (ปิด E12)

**Epic:** FAM-E12 · **สถานะ:** In Progress (branch feat/ai-import-map)
**ที่มา:** เกรพ — "ลุย 1068 .xls import ปิด E12" · ฟีเจอร์ AI ตัวสุดท้าย

## เป้าหมาย
หน้านำเข้า (FAM-1036) จับหัวคอลัมน์ไทยมาตรฐานได้เอง — แต่ไฟล์ export ที่ format เพี้ยน (อังกฤษ/สลับ/ชื่อแปลก) จับไม่ได้ · **AI ช่วยจับคู่คอลัมน์ต้นทาง → ฟิลด์รถ** แล้วนำเข้าต่อได้

## ทำอะไร (contained — ประมวลไฟล์อัปโหลด ไม่แตะ live data → ไม่มี RLS/money concern)
- **`lib/import/ai-map.ts`** (pure) — `AI_IMPORT_FIELDS` (10 ฟิลด์ + canonical header ตรง HEADER_MAP) · `parseColumnMap(raw, headers)` (JSON+ตัด fence, เก็บเฉพาะ field จริง + header ที่มีจริง — กัน AI มโนคอลัมน์) · `remapHeaders(rows, aiMap)` (เขียนหัวตารางเป็น canonical → `extractUnits` เดิมทำงานต่อไม่ต้องแก้) + tests
- **`lib/ai/prompts/import-map.ts`** (pure) — `columnMapPrompt(headers, sampleRows)` (ให้หัว+ตัวอย่าง 3 แถว → ตอบ JSON field→sourceHeader) + test
- **`app/api/ai/map-columns/route.ts`** — POST { headers, sampleRows } · guardAi → Claude → `parseColumnMap` (validate) → { mapping, mappedCount }
- **ImportView** — ปุ่ม "✨ ช่วยจับคู่คอลัมน์ (AI)" → ส่งหัว+ตัวอย่าง → ได้ mapping → `check(map)` ใช้ `remapHeaders` แล้ว preview · fix `onClick={check}`→`() => check()` (check รับ param แล้ว)

## Verify (/dev/imp :3010, ยังไม่มีคีย์)
- วางหัวอังกฤษ "Model,Colour,Engine No.,Cost" → ปุ่ม ✨ โผล่ → กด → POST → **401 "ยังไม่ได้ล็อกอิน" แสดงสุภาพ**
- route → 401 · **integration test: messy header + aiMap → remapHeaders → extractUnits ได้ units ถูก** (variantCode/colorName/engineNo)
- parseColumnMap เทส (ตัด fence, กัน field/header ปลอม) · tsc + eslint + vitest (298) + build ผ่าน
- **จับคู่จริง (AI อ่านหัวเพี้ยน) = เกรพเทสตอน login + ตั้งคีย์**

## 🎉 ปิด E12 (AI) — ครบ
Phase 0 infra (1065) · draft-followup (1066) · assistant tool-use RLS (1067) · **xls import map (1068)** · เหลือ enable = เจ้าของตั้ง ANTHROPIC_API_KEY + AI_ENABLED=true
