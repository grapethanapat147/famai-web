# FAM-1133 — เทสความปลอดภัยกับฐานข้อมูลจริง + เปิดใน CI (fixlist ข้อ 19 + 20)

**สถานะ:** Done (โค้ดเสร็จ · การรันจริงรอเจ้าของตั้ง secret)
**สาขา (git):** `feature/FAM-1133-security-tests`
**ต้องรัน SQL:** ไม่ต้อง

## ปัญหา
- ข้อ 19: เทส `auth-rls` เขียนไว้แล้วแต่ CI ข้ามตลอดเพราะไม่มีบัญชีทดสอบ → ช่องโหว่สิทธิ์หลุดขึ้นร้านได้โดยไม่รู้ตัว
- ข้อ 20: security-checklist มี 8 ข้อที่ระบุว่า "ต้องมีเทสจริงก่อนจบงาน" แต่ทุกช่องยังว่าง

## ทำอะไรบ้าง
**`tests/integration/security.test.ts`** — 5 เทสกับฐานข้อมูลจริง (skip เมื่อไม่ตั้ง `RUN_INTEGRATION=1`)
| เทส | ยืนยันอะไร | ต้องใช้ |
|---|---|---|
| anon อ่าน `pub.model` ได้ / เขียนไม่ได้ | เส้นแบ่ง public API | — |
| **ช่างกดขายรถ** → `sell_unit` ปฏิเสธ "ไม่มีสิทธิ์" และรถยังพร้อมขาย | ด่านอยู่ใน RPC ไม่ใช่ที่ปุ่ม | บัญชี tech |
| **ขายคันเดียวกัน 2 คำขอพร้อมกัน** → สำเร็จ 1 ล้มเหลว 1 · บิลขายมีใบเดียว · รถเป็น sold | row lock + unique กันขายซ้ำ | บัญชี sales/manager + service key |
| `next_doc_no` 5 ครั้งพร้อมกัน → ไม่ซ้ำ | ล็อกแถว doc_counter | บัญชี sales/manager |
| แก้ note รถ → มีแถว `audit_log` UPDATE พร้อมผู้แก้และค่าใหม่ | trigger บันทึกทุกการแก้ | บัญชี **admin** (RLS ให้แอดมินอ่านเท่านั้น) |

ข้อมูลทดสอบ (รถ `TEST-E-…`, ลูกค้า `TEST-…`) สร้างด้วย service role แล้ว**ลบทิ้งเองใน `afterAll`** เรียงตาม FK
บัญชีทดสอบใช้ของ `demo.sql` ได้เลย (`*.demo@famai.local` / `Famai@2569`) — ยกเว้น admin ต้องเป็นบัญชีจริง

**CI** — job `integration` ใหม่: รันเมื่อตั้ง secret `TEST_LOGINS` + `SUPABASE_SERVICE_ROLE_KEY` แล้วเท่านั้น
ยังไม่ตั้ง = ขึ้นข้อความบอกวิธีตั้ง ไม่แดง (ข้อ 19 ส่วนที่เจ้าของต้องทำ: ใส่ secret 2 ตัวใน GitHub → Settings → Secrets)

**security-checklist.md** — ติ๊ก 6/8 ข้อพร้อมชื่อเทส · 2 ข้อที่เหลือระบุเหตุผลชัด (customer mode ยังไม่มีในแอปนี้ · editBack ใช้สิทธิ์ตาม role แทน)

## ตรวจแล้ว
typecheck · lint · `npm test` (เทสชุดนี้ skip ตามออกแบบ) · build
**ยังไม่ได้รันกับฐานข้อมูลจริง** — ต้องรัน `demo.sql` ก่อน (บัญชีทดลอง) แล้ว `RUN_INTEGRATION=1 npx vitest run tests/integration` หรือให้ CI รันหลังตั้ง secret · ถ้ารันแล้วแดงส่งข้อความมา จะแก้ทันที
