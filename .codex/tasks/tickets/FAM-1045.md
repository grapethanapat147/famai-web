# FAM-1045 · /stock — ล้าง ?unit ออกจาก URL ตอนปิด drawer

**Epic:** FAM-E13 · **สถานะ:** Done (branch feat/stock-url-clean)
**ที่มา:** เกรพ — ต่อจาก FAM-1044 (deep-link) "ล้าง URL ตอนปิด drawer ด้วย"

## ทำอะไร
- StockView `closeDrawer()` — ปิดแผง + ลบเฉพาะ param `unit` ออกจาก URL ด้วย `history.replaceState`
  - เลือก replaceState (cosmetic) แทน router.replace → **ไม่ re-fetch/reload ตาราง** · เก็บ param อื่นไว้
  - เช็ค `has("unit")` ก่อน → กดปิดจาก row-click ปกติ (ไม่มี param) ไม่แตะ URL

## Verify (/dev/stock?unit=2)
- ก่อนปิด URL = `/dev/stock?unit=2` → กดปิด → URL = `/dev/stock` · drawer ปิด · ตารางไม่รีโหลด (FINN ยังอยู่) ✓
- tsc + eslint + build ผ่าน
