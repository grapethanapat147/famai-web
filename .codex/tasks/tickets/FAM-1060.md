# FAM-1060 · E10 cron — เตือนปิดงวดเงินเดือน (ปิด E10)

**Epic:** FAM-E10 · **สถานะ:** In Progress (branch feat/cron-payroll-close)
**ที่มา:** เกรพ — "ทำเตือนปิดเงินเดือนต่อเลย ปิด E10" (ต่อ FAM-1059) · **job สุดท้ายของ E10**

## เป้าหมาย
Cron job ที่ 5: เตือน HR ปิดงวดเงินเดือนช่วง **ใกล้สิ้นเดือน (เหลือ ≤ 3 วัน)** ถ้าเดือนนี้ยังไม่ปิดงวด → LINE ฝ่ายร้าน

## ทำอะไร (date-gated, read-only)
- **`lib/automation/clock.ts`** — `daysUntilMonthEnd(now)` + `yearMonth(now)` + tests
- **`lib/automation/payroll.ts`** (pure) — `isMonthClosed(periods, ym)` (มีงวดเดือนนี้ status ปิด/จ่ายแล้ว) + `shouldRemindPayroll(daysLeft, closed, window=3)` + tests
- **`payrollReminderMessage`** ใน line/message.ts — "💰 เหลืออีก N วันสิ้นเดือน — อย่าลืมปิดงวด…" + tests
- **`app/api/cron/payroll-close/route.ts`** — auth → อ่าน payroll_period → date-gate + closed-check → LINE · `?preview=1` ดูสถานะ · **รันได้ทุกวัน แต่ส่งเฉพาะ window + ยังไม่ปิด**
- **`database.types.ts`** — เพิ่ม `PayrollPeriod` + Table (เดิมตกหล่นเหมือน follow_up_task)
- **vercel.json**: cron `0 6 * * *` (13:00 ไทย)

## Verify (dev, วันนี้ = 21 → นอก window)
- ไม่มี auth → 401 · preview → `{daysLeft:10, closedThisMonth:false, wouldRemind:false}` (10 วัน > window 3 → ไม่เตือน ถูกต้อง) · real → `{remind:false, reason:"out-of-window"}`
- pure logic เทสเต็ม (daysUntilMonthEnd รวม ก.พ. 28 วัน, isMonthClosed, window boundary 0/2/10, message)
- tsc + eslint + vitest (280) + build ผ่าน · route = ƒ /api/cron/payroll-close

## 🎉 ปิด E10 — ครบ 5 cron
| job | เวลา | DB |
|-----|------|-----|
| aged-stock (รถค้าง) | 09:00 | read |
| registration-overdue (ทะเบียน) | 10:00 | read |
| service-due (เช็กระยะ) | 11:00 | write (mark) |
| sale-followup (งานติดตาม) | 12:00 | write (create) |
| payroll-close (เงินเดือน) | 13:00 | read |

foundation ร่วม: cron-auth (Bearer CRON_SECRET) · clock · LINE notify (no-op ถ้าไม่ตั้ง token) · ทุก job มี `?preview=1` · idempotent สำหรับ job ที่เขียน
