# FAM-1053 · E11 — generate og:image (การ์ดแบรนด์ต่อรุ่น)

**Epic:** FAM-E11 (Public Catalog & Status)
**สถานะ:** Done (branch feat/og-image)
**ที่มา:** เกรพ — "ทำ og:image generate ต่อ"

## ทำอะไร
รูป preview ตอนแชร์ LINE/เฟซ **สร้างสด** ด้วย `next/og` (ImageResponse/satori) → ลิงก์ดูดีแม้ยังไม่มีรูปรถจริง
- **`app/catalog/[code]/opengraph-image.tsx`** — การ์ด 1200×630 ต่อรุ่น: แบรนด์ (จุดแดง+ชื่อร้าน) · ประเภท·ซีซี·ปี · **ชื่อรุ่นไทยตัวใหญ่** · ราคา ฿ (แดง) · ป้ายสถานะ (เขียว/เหลือง) · footer Yamaha · ดึงข้อมูลจาก pub.model
- **`app/catalog/opengraph-image.tsx`** — การ์ดแคตตาล็อก landing ("แคตตาล็อกรถ Yamaha")
- ฟอนต์ไทย: ดึง **IBM Plex Sans Thai SemiBold TTF** จาก jsDelivr(gh:google/fonts) ตอน render (vendored woff2 satori ใช้ไม่ได้) · fetch fail → try/catch (og image เว้นได้แบบ graceful)
- metadata: ตัด conditional images ออก (opengraph-image.tsx auto-inject og:image + twitter:image) · twitter card = **summary_large_image** ทั้ง /catalog + /catalog/[code]

## Verify (localhost)
- /catalog/B6FU00 → `og:image` = /catalog/B6FU00/opengraph-image (1200×630, image/png 52KB) · **render จริงสวย**: "ฟินน์ ล้อแม็ก" + ฿46,900 + มีจำหน่าย (ไทยเรนเดอร์ถูกด้วย IBM Plex Sans Thai)
- /catalog/opengraph-image → 200 png 49KB ("แคตตาล็อกรถ Yamaha")
- twitter:card=summary_large_image + twitter:image ชี้ route เดียวกัน
- tsc + eslint + vitest (250) + build ผ่าน (opengraph-image เป็น route)

## ตามมา
- ฝังรูปรถจริงในการ์ด OG เมื่ออัปโหลดแล้ว (ตอนนี้เป็นการ์ดข้อความ) · จัดกลุ่ม trim รุ่นเดียว · ปุ่มแชร์
