# FAM-1069 · ถอดผู้ช่วย AI (E12) ออกจาก UI — เก็บโค้ดไว้ dormant

**Epic:** FAM-E12 · **สถานะ:** Done (branch feat/hide-ai-ui)
**ที่มา:** เกรพ — "ไม่ต้องมีผู้ช่วย AI เพราะ TOR ไม่มี · เขียนเตรียมไว้ได้ แต่บน UI ไม่ต้องเอาขึ้น กลัวลูกค้าเข้าใจผิดว่ามีให้"

## เป้าหมาย
ฟีเจอร์ AI (E12: ร่างข้อความ 1066 · ผู้ช่วยวิเคราะห์ 1067 · จับคู่คอลัมน์ 1068) **ไม่อยู่ใน TOR** → เอาออกจากหน้าจอทั้งหมด ไม่ให้ลูกค้า/เจ้าของงานเห็นแล้วเข้าใจว่าอยู่ใน scope · **แต่เก็บโค้ดไว้ (dormant)** เผื่ออนาคต เปิดกลับได้เร็วโดยไม่ต้องเขียนใหม่

## ทำอะไร (ถอด UI · ไม่ลบ logic)
- **`lib/nav/menu.ts`** — ลบเมนู `assist` ("ผู้ช่วย AI") · เมนู 22 → **21**
- **ลบหน้า** `app/(app)/assist/page.tsx` + `app/dev/assist/page.tsx` — ไม่มี route ให้เข้าถึงได้
- **`components/deal/DealView.tsx`** — ถอด `<DraftFollowUp>` + import + helper `followUpSituation` (คง `RegStage` ที่ใช้ที่อื่น)
- **`components/import/ImportView.tsx`** — ถอดปุ่ม "✨ ช่วยจับคู่คอลัมน์ (AI)" + state (aiMap/aiBusy/aiNote) + `aiMapColumns` + import `remapHeaders` · คืน `check()` เป็นรูปเดิม (ไม่มี param)
- **`tests/menu.test.ts`** — 22 → 21 + ตัด assertion `assist`

## เก็บไว้ dormant (ไม่แตะ)
`lib/ai/*` (config/client/guard/tools/prompts) · `app/api/ai/*` (ping/assist/draft-followup/map-columns — ยัง gate ด้วย AI_ENABLED=false) · `components/ai/*` (AssistChat/DraftFollowUp) · `lib/import/ai-map.ts` · เทส lib ที่ยัง valid (ai-assist / ai-import-map / followup)

## Verify
- tsc + eslint + vitest + build ผ่าน (เทส lib ที่เหลือยังพิสูจน์โค้ด dormant ว่าถูก)
- /dev/deal → ไม่มีปุ่มร่างข้อความ · /dev/imp → ไม่มีปุ่ม ✨ · เมนู admin ไม่มี "ผู้ช่วย AI" · /assist → 404

## เปิดกลับภายหลัง (ถ้า TOR เพิ่ม)
คืนเมนู 1 บรรทัด + คืน `<DraftFollowUp>`/ปุ่ม ✨ + สร้างหน้า assist กลับ → route/logic พร้อมใช้ทันที
