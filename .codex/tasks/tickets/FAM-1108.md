# FAM-1108 · เร็วขึ้น + UX/มือถือสม่ำเสมอ (จากผลตรวจ 5 มิติ — ชุดที่ 2)

**ประเภท:** (polish/perf) · **ที่มา:** ต่อจาก FAM-1107 (audit เดียวกัน) — แก้กลุ่ม perf + UX + มือถือจาก backlog ที่คัดแล้ว

## ทำ ✅ (branch feature/FAM-1108-perf-polish · stack ต่อจาก FAM-1107) — ไม่ต้อง migration

### ความเร็ว
- **Dashboard** (หน้าเปิดทุกวัน): scope 3 query ฝั่ง DB — unit เฉพาะคงเหลือ (.in status) · sale เฉพาะเดือนนี้ (.gte) · receivable เฉพาะค้าง (.is settled_at null) — เดิมดึงทั้งประวัติแล้วทิ้ง + จะชน cap 1000 แถวแล้วตัวเลขหน้าแรกเพี้ยนเงียบๆ
- **ศูนย์ซ่อม**: จำกัด 200 งานล่าสุด (ตารางโตเร็วสุดของร้าน) — lookup ลูกค้า/รถคงเดิม (เลี้ยง dropdown เปิดใบงาน)
- **ลงเวลา (หน้าใช้บ่อยสุด)**: ใบลาเหลือ รออนุมัติทุกใบ + ประวัติ 90 วัน (.or) · ยุบ waterfall 3 รอบ → 2 รอบขนานเต็ม
- **เงินเดือน**: getSettingsWith (query สด) → getSettings (แคชข้ามรีเควสต์)

### UX สม่ำเสมอ
- **ข้อความ error ภาษาผู้ใช้**: "รัน migration …?" (2 จุด) → "ระบบยังไม่พร้อม กรุณาแจ้งผู้ดูแลระบบ" · "(สิทธิ์บริษัท?)" (5 จุด) → "คุณอาจไม่มีสิทธิ์ในบริษัทนี้ ให้ผู้ดูแลตรวจสิทธิ์ที่หน้า บัญชีผู้ใช้"
- **฿ ท้ายเสมอ**: 4 จุดที่ ฿ นำหน้า (quote/sell ชิปของแถม/recv สรุป) → formatBaht
- **วันที่ พ.ศ. ทุกที่**: dash asOf + cron aged-stock + LINE label เดิมปี ค.ศ. → +543 · หน้าเช็กสถานะลูกค้า → formatThaiDate · วันครบเช็กระยะใน LINE เดิม ISO ดิบ → ไทย (อัปเดต 2 test assertion)
- **ปุ่ม modal ฟอร์ม**: "ปิด" → "ยกเลิก" 4 จุด (ทะเบียน 3 + แก้เวลา 1) ให้เหมือนทุก modal
- **"สาขา" ตัวสุดท้าย**: placeholder ส่งมอบ → "เช่น หน้าร้าน / บ้านลูกค้า" (FAM-1103 ครบ 100%)
- **Empty state**: หน้าลงเวลา (EmptyState + ปุ่มขอลา) · แคตตาล็อกลูกค้า (ปุ่มล้างคำค้น)

### มือถือ + a11y
- **ปุ่มแถวใหญ่ขึ้น** (RowButton py-1→py-2) ทะเบียน/แก้เวลา/บัญชี + gap ปุ่มบัญชี 1.5→2 + ดินสอ /models ขยาย hit area (p-2 -m-2)
- **กัน iOS ซูมเอง**: ช่องกรอกทั้งแอป text-sm (14px) → text-base (16px) — 17 ไฟล์ class เดียวกัน
- **inputMode**: เลขคำขอ/เลขเล่ม (numeric) · เลขภาษี (numeric) + โทร (tel) ใน modal แก้เอกสาร
- **Modal/Drawer focus**: เปิดแล้วย้ายโฟกัสเข้า dialog + Tab วนภายใน (focus trap เบา) + ปิดแล้วคืนโฟกัส — แก้ 2 ไฟล์ครอบคลุมทุกหน้า
- **next.config**: hostname รูป → โปรเจกต์จริง xpbdvhfvmpokdnfhoujx (เดิมชี้โปรเจกต์เก่าที่เลิกใช้)

## Verify
- สวีทเต็ม 435 ผ่าน · typecheck · lint (แก้ react-hooks/purity: Date.now → helper) · build เขียว
- เทสจริง /dev/registration: ปุ่มใหญ่ขึ้น + modal ส่งมอบ = ปุ่ม "ยกเลิก" + placeholder ใหม่ + **focus เข้า dialog จริง** ✓

## Backlog เหลือ (ตัดสินใจภายหลัง — ไม่บล็อกอะไร)
- scope query หน้า report/expense/ar/transfer/quote/cal (medium effort, ผลตอนข้อมูลโต)
- เลือกลูกค้าเดิมตอนขาย (FAM-1023 — กันระเบียนซ้ำ, ต้องแก้ RPC)
- แอดมินสร้างผู้ใช้จากแอป (ต้อง service-role admin API) · หน้าตั้งเงินเดือนพนักงาน (payroll ใช้ base_salary ที่ยังไม่มีที่กรอก)
- ลิงก์ดูดีลจากหน้าเงินค้างรับ
