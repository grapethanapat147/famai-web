## FAM-1003: Core UI components

Status: In Progress
Priority: High
Epic: FAM-E02
Refs: docs/04-design-system.md §11.3 · index.html (v1.15) · Spec §11.3-11.4

### Summary
คอมโพเนนต์ที่ใช้ซ้ำทุกหน้า (จาก §11.3): การ์ด KPI, แถบตัวกรอง (+สรุปว่าดูอะไรอยู่), ตารางกดได้ → แผงรายละเอียดสไลด์จากขวา (ปิดด้วยปุ่ม/พื้นหลัง/Esc), หน้าต่างซ้อน (modal), ป้ายสถานะ 5 แบบ (ดี/เตือน/แย่/ข้อมูล/ปิด), ป้ายที่มาข้อมูล (`ข้อมูลจริง`/`ข้อมูลสมมุติ`), บนมือถือ: ตาราง→การ์ด, แท็บ→ชิป, ตัวกรอง→แผ่นล่าง (v1.4)

### Acceptance Criteria
- แต่ละคอมโพเนนต์ typed + มี stories/ตัวอย่าง + test พฤติกรรม (Esc ปิด drawer, ป้ายแยกด้วยตาเปล่า)
- ยึดกฎ §11.4: ทุกตัวเลขบอกที่มาได้, preview ก่อนบันทึก, คำอธิบาย ≤ 1 บรรทัด, กดดูรายละเอียดได้เสมอ
- มือถือ: ไม่มี horizontal scroll, ตารางแปลงเป็นการ์ด

### Validation
- component tests (drawer/modal/badge/filter) ผ่าน
- เทียบหน้าตากับ `index.html` จอที่ใช้ component นั้น

### Design Reference
`index.html` — KPI card, filter bar, detail drawer, chips · §11.3-11.4

### Implementation Notes (ready for review)
คอมโพเนนต์ตาม docs/04 (tokens/§4 surface/§7 rules/§8 mobile):
- `lib/format.ts` — `formatBaht` (คั่นพัน, ติดลบวงเล็บ), `formatThaiDate` (พ.ศ., timezone-safe), `formatPercentChange` (ช่วงก่อนไม่มีข้อมูล ไม่โชว์ 0%/∞) + tests
- `components/ui/` — `Money` (ติดลบแดง, ไม่มีสิทธิ์→'—'), `StatusBadge` (5 แบบ text+dot), `DataSourceBadge` (จริง/สมมุติ), `StatCard` (KPI, ตัวเลข fluid ไม่ตกบรรทัด §8), `Drawer` (ขวา/มือถือ bottom-sheet, Esc/backdrop/scroll-lock), `Modal`, `Chips` (แท็บ→ชิปทึบ), `FilterBar` (เดสก์ท็อปแถวเดียว/มือถือแผ่นล่าง + สรุป), `DataTable<T>` (ตาราง→การ์ดบนมือถือ, กดแถว→drawer, a11y keyboard)
- `app/dev/ui` + `components/dev/UiKitDemo` — โชว์เคสไว้ตรวจ/อ้างอิง (ไม่อยู่ในเมนู)

Validation: `tsc` ✅ · `eslint` ✅ · `vitest` ✅ 21/21 · `next build` ✅ (25 หน้า)
**ตรวจจริงบนเบราว์เซอร์**: เดสก์ท็อป KPI/badges/ตาราง + กดแถว→drawer ขวา (วันที่ พ.ศ. ถูก) · มือถือ 375px: KPI 2-col ตัวเลขไม่ตกบรรทัด, ตาราง→การ์ด, ตัวกรองยุบเป็นปุ่ม, **ไม่มี horizontal scroll**; money-strip toggle ซ่อนต้นทุนเป็น '—'

ready for review. ไอคอน/คอมโพเนนต์เพิ่มเติมทำเสริมได้ตามหน้าจอที่ต้องใช้
