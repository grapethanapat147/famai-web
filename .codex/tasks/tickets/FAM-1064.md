# FAM-1064 · E08 — polish ArView (เงินค้างรับ)

**Epic:** FAM-E08 · **สถานะ:** Done (branch feat/ar-polish)
**ที่มา:** เกรพ — "polish ArView เงินค้างรับต่อเลย"

## แนวคิด
ArView มีสรุป 3 ตัว (ค้างทั้งหมด/เกินกำหนด/รายการค้าง) แต่ใช้การ์ด `Stat` mini เฉพาะที่ (เล็ก, ไม่ตรงกับ hero ที่อื่น) → รวมเป็น StatCard เดียวกับ Dashboard/Parts/Deal + เพิ่ม "จำนวน" ที่เกินกำหนด

## ทำอะไร
- **`lib/ar/receivables.ts`** — `arTotals` เพิ่ม `overdueCount` (นับรายการเกินกำหนด) + test
- **ArView** — แทน `Stat` mini ด้วย **StatCard** ที่ใช้ทั้งแอป:
  - ค้างทั้งหมด (+ hint "N รายการ")
  - **เกินกำหนด** — tone="accent" (วงแหวน+ตัวเลขแดง) + hint "N รายการ · ทวงถามด่วน" (โชว์ทั้งยอด+จำนวน+ความเร่งด่วน)
  - รายการค้าง (+ hint "รายการ")
- ลบ `Stat` component ที่ตายแล้ว · grid responsive 2→3 คอลัมน์

## Verify (/dev/ar)
- hero: ค้างทั้งหมด 280,900฿ (3 รายการ) · **เกินกำหนด 84,000฿ (accent) · 1 รายการ · ทวงถามด่วน** · รายการค้าง 3
- mobile 375 ไม่มี h-scroll (2 คอลัมน์) · overdueCount เทส · tsc + eslint + vitest (283) + build ผ่าน

## E08 สถานะ
pipeline + off-track (1061) + ประวัติซื้อ (1062) + ประวัติบริการ (1063) + **AR polish (ใบนี้)** — E08 เก็บครบเชิงฟังก์ชัน
