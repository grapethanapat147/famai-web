# FAM-1135 — อัปเดตจำนวน migration ใน README

**สถานะ:** Done
**สาขา (git):** `chore/FAM-1135-readme-migration-count`
**ต้องรัน SQL:** ไม่ต้อง

> ตั๋วนี้เขียนย้อนหลังใน FAM-1140 — งานเข้า main ไปแล้ว (PR #137) แต่ตอนนั้นไม่ได้สร้างไฟล์ตั๋วไว้
> จึงเป็นตั๋วเดียวใน main ที่ไม่มีไฟล์ ทำให้กระดานงานประกอบกลับจาก `git log` ได้ไม่ครบ

## ปัญหา
README เขียนว่า `supabase/migrations/` มี 34 ไฟล์ (หมายเลขล่าสุด 35) ทั้งที่ตอนนั้นมี 36 ไฟล์ (ล่าสุด 37)
ตัวเลขนี้เป็นตัวเลขที่เกรพใช้ตรวจว่ารัน SQL ครบหรือยัง ถ้าผิดก็ตรวจไม่ได้

## ทำอะไรบ้าง
แก้บรรทัดเดียวใน README ให้เป็น 36 ไฟล์ (หมายเลขล่าสุด 37)

## ตามมาทีหลัง
ตัวเลขชุดนี้ค้างอีกรอบทันทีที่ migration 38 เข้ามาใน FAM-1137
FAM-1140 จึงใส่เทสต์ `tests/docs-migrations.test.ts` ให้ README กับโฟลเดอร์ migration ตรงกันเสมอ
จะได้ไม่ต้องมีตั๋วแบบนี้อีก
