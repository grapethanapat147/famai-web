# Famai Motor Group — ระบบหลังบ้าน

ระบบหลังบ้านสำหรับร้านมอเตอร์ไซค์ยามาฮ่า **3 บริษัทในเครือเดียวกัน** ใช้งานผ่านอินเทอร์เน็ต เชื่อมข้อมูลข้ามบริษัท
เปิดใช้ที่ https://famai-web-three.vercel.app · อัปเดต 3 ก.ย. 2569

| บริษัท | รหัส | เลขเอกสารขึ้นต้น |
|---|---|---|
| Famai Motor Group (สำนักงานใหญ่) | `FMG01` | `FMG-` |
| Famai Motor | `FMM01` | `FMM-` |
| Famai Center Group | `FCG01` | `FCG-` |

> คำว่า **"บริษัท"** ในแอปหมายถึง 3 รหัสข้างบน (ตาราง `branch`) · ทั้งสามอยู่ใต้นิติบุคคลเดียวกัน (ตาราง `company`)
> โอนรถระหว่างบริษัทในเครือทำได้ · ข้ามนิติบุคคลต้องเปิดบิล **ขายส่ง (B2B)**

---

## ระบบทำอะไรได้บ้าง — 28 หน้า 6 กลุ่ม

สิทธิ์เห็นหน้าไหนขึ้นกับบทบาท 7 แบบ: **admin** ผู้ดูแลระบบ · **manager** ผู้บริหาร · **sales** เซลล์ · **stock** สต๊อก · **acct** บัญชี · **hr** ฝ่ายบุคคล · **tech** ช่าง
(ตัวเลขเงิน เช่น ต้นทุน/กำไร เห็นเฉพาะบทบาทที่มีสิทธิ์ `money` — ซ่อนตั้งแต่ฝั่งเซิร์ฟเวอร์ ไม่ใช่แค่ซ่อนปุ่ม)

| กลุ่ม | หน้า | ทำอะไร | ใครเห็น |
|---|---|---|---|
| ภาพรวม | Dashboard | ยอดขายเป็นเงินวันนี้/เดือนนี้ · เงินเข้า−ออกวันนี้ · อัตราไฟแนนซ์อนุมัติ · รุ่นใกล้หมด · รถค้างสต๊อก (ปรับการ์ดได้) | ทุกคน |
| | ปฏิทิน | ทะเบียนครบกำหนด · นัดเช็กระยะ · งานติดตามลูกค้า · อีเวนท์บริษัท | admin manager acct sales hr |
| | รายงาน | ยอดขาย (รุ่น/บริษัท/เซลล์/เดือน) · **อะไหล่ (ยอด/ต้นทุน/กำไรรายวัน)** · ค่าใช้จ่าย · เงินค้างรับ — พิมพ์/CSV | admin manager acct stock hr |
| มอเตอร์ไซค์ | รับรถเข้าสต๊อก | บันทึกทีละคัน เลขเครื่อง/เลขถัง ต้นทุน | admin manager stock |
| | สต๊อกรถ | ค้นหา จัดการ ตั้งราคา ดูอายุสต๊อก | admin manager stock sales acct |
| | ขายส่ง (B2B) | ร้านค้าปลายทาง · บิลเดียวหลายคัน · ขายเชื่อตั้งเงินค้างรับให้ · ใบกำกับ · ยกเลิกบิล | admin manager sales |
| | ขายรถ | เปิดการขาย เงินสด/ผ่อน · ของแถมจากคลัง (ตัดสต๊อก) · เลือกลูกค้าเดิมได้ | admin manager sales |
| | โอนย้ายบริษัท | ย้ายรถระหว่างบริษัทในเครือ (ข้ามนิติบุคคลถูกกัน) | admin manager stock |
| | ใบเสนอราคา | เทียบรถ 2 คัน + ค่างวดไฟแนนซ์ พิมพ์ให้ลูกค้า | admin manager sales |
| ลูกค้าและการเงิน | ลูกค้าและดีล | ลีด (เข้ามาดูรถ→สนใจ→ทำสัญญา) → ดีล 4 เฟส **คุยกับลูกค้า → ไฟแนนซ์ → เปิดการขาย → ส่งมอบ** · ประวัติซื้อ/ซ่อม · รหัสเช็กสถานะ | admin manager acct sales |
| | งานทะเบียน | คิวจดทะเบียน/ป้าย เรียงตามค้างนาน | admin manager acct |
| | บัญชี | ใบเสร็จ/ใบกำกับ · **ขายผ่อน = ชุด 3 ใบ** (ใบเสร็จเงินดาวน์ · ใบกำกับเงินดาวน์ชื่อลูกค้า · ใบกำกับยอดจัดชื่อไฟแนนซ์) · แก้ไข/ยกเลิก/พิมพ์ | admin manager acct |
| | ใบกำกับภาษี | หน้าบัญชีที่กรองมาเฉพาะใบกำกับ | admin manager acct |
| | เงินค้างรับ | ไฟแนนซ์ / ลูกค้า / ร้านค้าขายส่ง — ลงรับเงินตัดยอด | admin manager acct |
| บริการและอะไหล่ | ศูนย์ซ่อม | ใบงานทุกสถานะ · ค้นเลขเครื่อง/เลขถัง · ประวัติลูกค้าในใบงาน · ปิดงานแล้วนัดเช็กระยะรอบถัดไปให้ | admin manager tech stock |
| | อะไหล่และของแถม | คลังอะไหล่ รับเข้า/เบิก/ขาย · ของแถม (ฟอร์มขายดึงจากที่นี่) · เตือนของใกล้หมด | admin manager stock tech acct sales |
| | ค่าใช้จ่าย | บันทึกรายจ่าย + อนุมัติ + ธงใบเสร็จหาย | admin manager acct |
| บัญชีและพนักงาน | ภาพรวมการเข้างาน | ใครมา/สาย/ขาด · เซลฟี่ · จุดที่ลงเวลา · แก้เวลาย้อนหลัง | admin manager hr |
| | ลงเวลาและลา | ลงเวลาเข้า/ออก (GPS + เซลฟี่) · OT คิดให้อัตโนมัติ · ขอลา/อนุมัติ | ทุกคน |
| | พนักงาน | ตำแหน่ง เงินเดือน เลข ปกส. บัญชีธนาคาร · เพิ่มพนักงานใหม่ | admin manager hr |
| | เงินเดือนและ OT | สลิป · คอมมิชชั่น · **ปิดงวด (แช่ยอด)** · ใบนำส่ง ปกส. · ไฟล์โอนธนาคาร | admin manager hr acct |
| ข้อมูลและตั้งค่า | บัญชีผู้ใช้ | ผู้ใช้ บทบาท สิทธิ์ | admin |
| | รุ่นรถและสี | แคตตาล็อกรุ่น สี ราคา รูป (ขึ้นเว็บขายผ่าน `pub` API) | admin manager |
| | นำเข้าข้อมูล | อ่านไฟล์ .xls จากระบบยามาฮ่า | admin manager stock |
| | สาขา | จุดลงเวลาของแต่ละบริษัท (พิกัด + รัศมี หลายจุดได้) | admin manager |
| | ประวัติการแก้ไข | ใครแก้อะไรเมื่อไหร่ (ฐานข้อมูลบันทึกให้อัตโนมัติ) | admin |
| | ตั้งค่าระบบ | เกณฑ์ทั้งหมด (VAT รอบติดตาม เวลางาน OT ปกส. …) · ข้อมูลบริษัท · ไฟแนนซ์ · ธีม | admin manager |
| | ผังกระบวนการ | ใครทำอะไรตอนไหน ทุกสายงาน | ทุกคน |

หน้าสำหรับลูกค้าที่ไม่ต้องล็อกอิน: `/status` เช็กสถานะรถด้วยรหัสติดตามที่พิมพ์ท้ายใบเสร็จ · แคตตาล็อกรุ่นผ่าน API ใน `docs/07-public-api.md`

**งานอัตโนมัติ (cron บน Vercel ทุกคืน):** เตือนรถค้างสต๊อก · ทะเบียนใกล้ครบกำหนด · ถึงกำหนดเช็กระยะ (LINE) · งานติดตามลูกค้า · เตือนปิดงวดเงินเดือน

---

## เทคโนโลยี

Next.js 16 (App Router) + React 19 + TypeScript · Tailwind + design tokens (`docs/04-design-system.md`) · Supabase (Postgres + RLS + Auth + Storage) · Deploy Vercel (region `sin1`)

หลักที่ยึดทั้งระบบ (ดูรายละเอียดใน `CLAUDE.md`): ด่านสิทธิ์อยู่ในฟังก์ชันที่เขียนข้อมูล ไม่ใช่ที่ปุ่ม · แยกข้อมูลบริษัทด้วย RLS ที่ฐานข้อมูล · งานเงิน/ตัดสต๊อกทำผ่าน RPC ในทรานแซกชันเดียว (`sell_unit` · `sell_wholesale` · `next_doc_no` …) · ไม่ฝังเกณฑ์ในโค้ด อ่านจาก `app_setting`

---

## รันบนเครื่อง

```bash
npm ci
cp .env.example .env.local     # เติมค่า Supabase (ดูคีย์ในไฟล์)
npm run dev                    # http://localhost:3000
```

ตรวจก่อน push (เท่ากับที่ CI รันทุก PR): `npm run test` · `npm run typecheck` · `npm run lint` · `npm run build`

หน้า `/dev/*` (เช่น `/dev/deal`, `/dev/payroll`) เป็นพรีวิวแต่ละหน้าด้วยข้อมูลจำลอง ไม่ต้องล็อกอิน — ใช้ดูหน้าตา/ทดสอบฟอร์มได้โดยไม่แตะฐานข้อมูล

---

## ฐานข้อมูล

- โปรเจกต์ Supabase `famai-motor` ref `xpbdvhfvmpokdnfhoujx`
- `supabase/migrations/` — **37 ไฟล์ (หมายเลขล่าสุด 38) รันครบแล้ว** · ไฟล์ใหม่ต้องเปิด Supabase → SQL Editor แล้ววาง **เนื้อ SQL** ทั้งก้อน (ไม่ใช่ชื่อไฟล์) กด Run · รันเรียงตามชื่อไฟล์
- `lib/supabase/database.types.ts` เขียนด้วยมือเฉพาะคอลัมน์ที่แอปใช้ — เพิ่มคอลัมน์ในฐานข้อมูลแล้วต้องเพิ่มที่นี่ด้วย ไม่งั้นโค้ดจะ resolve เป็น `never`

### ข้อมูลตัวอย่าง (ให้เห็นทุกหน้าว่าหน้าตาเป็นอย่างไรเมื่อมีข้อมูล)

`supabase/seed/demo.sql` ใส่เรื่องราวเดียวกันทั้งระบบ: ลูกค้าคนเดิมโผล่ตั้งแต่ลีด → ขาย → ไฟแนนซ์ → ทะเบียน → เอกสาร → รอรับเงิน → ศูนย์ซ่อม → เตือนเช็กระยะ · พนักงานคนเดิมโผล่ใน ลงเวลา → OT → เงินเดือน (งวด ส.ค. ปิดแล้ว) → ใบนำส่ง ปกส. · บิลขายส่ง · โอนย้าย · ใบเสนอราคา · ค่าใช้จ่าย · ปฏิทิน

| | |
|---|---|
| ใส่ | วางเนื้อ `supabase/seed/demo.sql` ใน SQL Editor แล้ว Run (รันได้ครั้งเดียว ถ้ามีอยู่แล้วจะหยุดพร้อมบอกให้ล้างก่อน) |
| บัญชีทดลอง | 8 คน ครบทั้ง 7 บทบาท เช่น `admin.demo@famai.local` · `manager.demo@famai.local` · `sales1.demo@famai.local` — รหัสผ่านเดียวกัน `Famai@2569` |
| ลบทิ้ง | วางเนื้อ `supabase/seed/demo-cleanup.sql` แล้ว Run — ลบเฉพาะแถวตัวอย่าง (id ขึ้นต้น `0000000d-e000-…` · รถเลขเครื่อง `DEMO-`) ข้อมูลจริงไม่ถูกแตะ |

**ก่อนเปิดใช้จริงต้องรัน `demo-cleanup.sql`** เพราะบัญชีทดลองรหัสผ่านเดียวกันหมด

---

## คู่มือพนักงาน (PDF 8 เล่ม)

`docs/manual/` — 7 เล่มตามบทบาท (ผู้ดูแล · ผู้บริหาร · เซลล์ · สต๊อก · บัญชี · ฝ่ายบุคคล · ช่าง) + 1 เล่มผังกระบวนการ
แต่ละเล่มบอก **หน้าที่บทบาทนั้นเห็น · สิทธิ์ · งานประจำวันทีละขั้น · ตัวอย่างที่เห็นบนจอ · จุดที่มักพลาด · สายงานที่เกี่ยวข้อง**
ปิดท้ายด้วยภาคผนวกภาพหน้าจอจริง

| | |
|---|---|
| อ่านบนจอ | `/dev/manual` แล้วเลือกเล่ม (ไม่ต้องล็อกอิน · สั่งพิมพ์จากเบราว์เซอร์ได้เลย) |
| แก้เนื้อหา | `lib/manual/manual.ts` — โครงหน้า/สิทธิ์/ผังงานดึงจาก `MENU` กับ `FLOWS` เอง ไม่ต้องแก้ซ้ำ |
| สร้าง PDF ใหม่ | `node tools/manual/build.js` (ถ้ามี dev รันอยู่แล้วให้ใช้ `--port 3010` จะเร็วกว่ามาก) |

ใช้ Chrome ที่ติดตั้งในเครื่องพิมพ์ ไม่ต้องลงแพ็กเกจเพิ่ม · ตัวหนังสือใน PDF ค้นหาและคัดลอกได้ (ไม่ใช่ภาพ)

---

## ก่อนเปิดใช้จริง — สิ่งที่เจ้าของต้องกดเองบน Supabase/GitHub

1. **รีเซ็ตรหัสผ่านฐานข้อมูล** (Settings → Database) — รหัสเดิมเคยส่งผ่านแชต
2. **เปิด 2FA** ที่บัญชี Supabase (Account preferences → Security) — ทำได้ทุกแพ็ก
3. **สำรองข้อมูลเอง** ตราบใดที่ยังใช้แพ็กฟรี (ฟรีไม่มี backup อัตโนมัติ) — `npm run backup` เดือนละครั้ง แล้วก๊อปไฟล์ออกนอกเครื่อง
   ครั้งแรกต้อง `brew install libpq && brew link --force libpq` และเติม `DATABASE_URL` ใน `.env.local`
   **อัปเกรด Pro ก่อนออกใบกำกับภาษีจริงใบแรก** — จะได้ backup อัตโนมัติ + เปิด Leaked Password Protection ได้ (ฟีเจอร์นั้นมีเฉพาะ Pro)
4. ตั้ง `DEMO_LOGIN=false` บน Vercel (ถ้าลืม ระบบปิดให้เองบน production และขึ้นเตือนที่หน้า login แต่ควรตั้งให้ถูก)
5. ใส่บัญชีทดสอบเป็น secret ใน GitHub Actions + `RUN_INTEGRATION=1` เพื่อให้เทสตรวจสิทธิ์กับฐานข้อมูลจริง (`tests/integration/auth-rls.test.ts`) รันทุกครั้ง
6. กรอก **เลขผู้เสียภาษีของบริษัทไฟแนนซ์** (ตั้งค่าระบบ) และ **ร้านค้าขายส่ง** — ไม่กรอกจะออกใบกำกับให้เขาไม่ได้
7. รัน `demo-cleanup.sql`
8. เมื่อพร้อมเปิดแคตตาล็อกให้ลูกค้าค้นเจอใน Google: ตั้ง `CATALOG_PUBLIC=true` บน Vercel แล้ว deploy ใหม่ (ก่อนหน้านั้นทุกหน้า noindex)

---

## โครงสร้างรีโป

```
app/(app)/<หน้า>/page.tsx + actions.ts   หน้าจริง (server) + server actions
app/dev/<หน้า>/page.tsx                 พรีวิวด้วยข้อมูลจำลอง (ไม่ต้องล็อกอิน)
app/api/cron/*                          งานอัตโนมัติ (ตาราง crons ใน vercel.json)
components/<หน้า>/                      UI (client)
lib/<โดเมน>/                            ตรรกะบริสุทธิ์ (ทดสอบได้ ไม่แตะ DB)
tests/                                  vitest
supabase/migrations/                    SQL ทั้งหมด (รันเองใน SQL Editor)
supabase/seed/                          ข้อมูลตัวอย่าง + ตัวลบ
scripts/backup.sh                       สำรองฐานข้อมูลด้วยมือ (แพ็กฟรีไม่มี backup อัตโนมัติ)
.codex/tasks/                           กระดานงาน (ticket-first) · done.md sync จาก git
docs/                                   เอกสารออกแบบ (ดู docs/README.md ว่าฉบับไหนยังใช้ได้)
docs/manual/                            คู่มือพนักงาน PDF 8 เล่ม
tools/manual/build.js                   สั่ง Chrome พิมพ์ /dev/manual/* เป็น PDF
index.html · prototype/ · tools/qa/ · reference/   ต้นแบบเดิม v1.15 — อ้างอิงเท่านั้น ไม่ใช่ตัวจริงแล้ว
```

การทำงาน: ทุกงานมีตั๋ว `FAM-XXXX` ใน `.codex/tasks/tickets/` → สาขา `feature/FAM-XXXX` → PR → เจ้าของ merge · รายละเอียดกติกาสำหรับผู้ช่วย AI อยู่ใน `CLAUDE.md`
